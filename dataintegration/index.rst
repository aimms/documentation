Data Integration with AIMMS
================================================

Use this helper to find the right data integration method for your situation. Hover over the options to read more information about them.

This helper covers **AIMMS Cloud accounts only**. For on-premise setups, see the `AIMMS PRO documentation <https://documentation.aimms.com/pro/index.html>`_.

.. raw:: html

	<style>
	#di-helper{font-family:Aimms-Avenir,'futura-pt',sans-serif;max-width:680px;margin:0 auto;padding:28px 32px;background:#fff;border:1px solid #e1e4e5;border-radius:5px;box-shadow:0 0 11px 0 rgba(0,0,0,.06);box-sizing:border-box}
	#di-helper .di-hint{color:#909aae;font-size:.88em;margin-bottom:18px}
	#di-helper .di-hd{font-size:1.15em;font-family:Aimms-Avenir-Medium,'futura-pt',sans-serif;color:#1847c9;border-bottom:3px solid #1847c9;padding-bottom:8px;margin-bottom:16px}
	#di-helper .di-content{color:#909aae;font-size:.85em;line-height:1.55;margin-bottom:14px;background:#f5f6fa;border-radius:4px;padding:7px 12px}
	#di-helper .di-content a{color:#1847c9}
	#di-helper .di-content ul{padding-left:22px;margin:10px 0}
	#di-helper .di-content ol{padding-left:22px;margin:10px 0}
	#di-helper .di-q{font-size:1.05em;font-family:Aimms-Avenir-Medium,'futura-pt',sans-serif;color:#404040;margin-bottom:18px}
	#di-helper .di-opts{display:flex;flex-wrap:wrap;gap:10px}
	#di-helper .di-btn{background:#1847c9;color:#fff;border:none;border-radius:1rem 1rem .8rem;padding:12px 16px;font-size:.9em;font-family:Aimms-Avenir-Medium,'futura-pt',sans-serif;letter-spacing:.03em;cursor:pointer;flex:1 1 160px;text-align:center;line-height:1.4;transition:background .15s}
	#di-helper .di-btn:hover{background:#133a9f}
	#di-helper .di-res{color:#404040;line-height:1.7}
	#di-helper .di-res a{color:#1847c9}
	#di-helper .di-res ul{padding-left:22px;margin:10px 0}
	#di-helper .di-res ol{padding-left:22px;margin:10px 0}
	#di-helper .di-res p{margin:0 0 10px}
	#di-helper .di-admon{background:#e5f5fb;border-radius:4px;margin:14px 0;overflow:hidden}
	#di-helper .di-admon.di-admon-see{background:#e7f2fa}
	#di-helper .di-admon-title{background:#1847c9;color:#fff;font-family:Aimms-Avenir-Medium,'futura-pt',sans-serif;font-size:.9em;font-weight:700;padding:8px 12px 6px;margin:0;display:block}
	#di-helper .di-admon.di-admon-see .di-admon-title{background:#6ab0de}
	#di-helper .di-admon-title::before{font-family:FontAwesome;content:"\f06a";margin-right:4px}
	#di-helper .di-admon-body{padding:12px 14px}
	#di-helper .di-admon-body>*:first-child{margin-top:0}
	#di-helper .di-admon-body>*:last-child{margin-bottom:0}
	#di-helper .di-admon-body ul{padding-left:0;list-style-position:inside;margin:8px 0}
	#di-helper .di-admon-body ol{padding-left:0;list-style-position:inside;margin:8px 0}
	#di-helper .di-admon-body a{color:#1847c9}
	#di-helper .di-admon-body p{margin:0 0 8px}
	#di-helper .di-nav{margin-top:20px;display:flex;gap:10px}
	#di-helper .di-nav button{background:#fff;border:1px solid #1847c9;border-radius:1rem 1rem .8rem;padding:7px 18px;cursor:pointer;font-size:.88em;font-family:Aimms-Avenir,'futura-pt',sans-serif;color:#1847c9;letter-spacing:.03em}
	#di-helper .di-nav button:hover{background:#f5f6fa}
	#di-helper .di-crumb{display:flex;flex-wrap:wrap;align-items:center;gap:2px 0;margin-bottom:16px;padding-bottom:12px;border-bottom:1px solid #e1e4e5;font-size:.8em;line-height:1.6}
	#di-helper .di-crumb-item{color:#1847c9;font-family:Aimms-Avenir-Medium,'futura-pt',sans-serif;max-width:240px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
	#di-helper .di-crumb-sep{color:#909aae;padding:0 5px}
	</style>
	<div id="di-helper"></div>
	<script>
	(function(){
	var T={
  "1": {type: 'q', q: `Do you want to import data into the AIMMS Cloud, or export data from it?`, c: [{l: `Import data into AIMMS`, to: `2`, tip: `You want to get data from your side into the AIMMS cloud, for example into an AIMMS application.`}, {l: `Export data from AIMMS`, to: `3`, tip: `You want to export data (e.g. results of the optimization) to your own data platform.`}]},
  "2": {hd: `Import data into`, type: 'q', q: `Where is the data you want to import coming from? Hover over the options to read more information.`, c: [{l: `The data is stored in files I manually upload`, to: `4`, tip: `E.g. the data is in an Excel file. If you are exporting this data from another application/ERP first, you might consider building a direct implementation between the two sources`}, {l: `I am using an external application/ERP that holds the data`, to: `12`, tip: `E.g. SAP/Oracle`}, {l: `The data is stored in a data warehouse, datalake or alike`, to: `6`, tip: `E.g. Azure DataLake, Snowflake, Google Cloud storage`}, {l: `The data is stored in a database on a server`, to: `8`, tip: `E.g. you have a database on a server within your company's network`}, {l: `The data is stored on an Azure platform and I want a direct connection to the AIMMS Azure platform`, to: `10`, tip: `If your setup is fully built on Azure, it might make sense to investigate if a direct connection to the AIMMS Cloud (also running on Azure) is a viable option`}, {l: `The data is already stored on the AIMMS Cloud`, to: `9`, tip: `In the AIMMS Cloud MySQL database, Azure Data Lake Storage or in the PRO Storage`}]},
  "3": {hd: `Export data from`, type: 'q', q: `Where is the data you want to export stored?`, c: [{l: `The data is stored on the Azure Data Lake Storage Gen2 (as part of the AIMMS Cloud)`, to: `23`, tip: `You are utilizing the AIMMS Cloud ADLS for storing your data`}, {l: `The data is only available within the AIMMS model`, to: `24`, tip: `Results of optimizations are solely available from within the AIMMS application`}, {l: `The data is stored on the AIMMS PRO Storage`, to: `25`, tip: `You are storing the results on the AIMMS PRO Storage (using pro:: methods)`}, {l: `The data is stored in the AIMMS Cloud database`, to: `26`, tip: `There is a MySQL database available in your AIMMS Cloud and you are storing the data on there`}, {l: `I want a direct connection from the AIMMS Azure platform to my Azure platform`, to: `27`, tip: `You have an Azure environment and want a direct link to the AIMMS Azure platform`}]},
  "4": {hd: `The data is stored in files I manually upload`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>You want to use data in your AIMMS application that is stored in an external file;</li><li>and you want to use this data in the AIMMS model of the application.&nbsp;</li></ul><p><strong>Your to-do list to implement this route</strong>:</p><p>If the data is stored in files you manually upload, or the data you want to retrieve is not available through REST API, it is best to use the AIMMS WebUI to upload the files. Take a look at the <a href="https://documentation.aimms.com/webui/upload-widget.html" rel="noopener noreferrer" target="_blank" title="">WebUI Upload Widget</a>, which you will probably want to combine with <a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">a mapping</a> (as made available by the Data Exchange Library) to map the data correctly onto your AIMMS model. A full example can be found <a href="https://how-to.aimms.com/Articles/383/383-contract-allocation.html" rel="noopener noreferrer" target="_blank" title="">here</a>.</p>`},
  "6": {hd: `The data is stored in a data warehouse, datalake or alike`, type: 'q', content: `<p>Examples could be:</p><ul><li>Azure Data Lake</li><li>Snowflake</li><li>Google Cloud</li></ul>`, q: `Can the data be exposed through REST API and is it available in Parquet, CSV, XLS, JSON or database format?`, c: [{l: `Yes, it can be exposed through REST API in one of those formats`, to: `13`, tip: ``}, {l: `I want to use external API's to push the data`, to: `30`, tip: ``}, {l: `No, it cannot be exposed through REST API or not in any of those formats`, to: `4`, tip: ``}]},
  "8": {hd: `The data is stored in a database within a secured network`, type: 'q', q: `Which scenario applies to this database?`, c: [{l: `The database is publicly available`, to: `20`, tip: ``}, {l: `The database can be whitelisted via IP`, to: `21`, tip: ``}, {l: `We'll need a VPN setup for a direct connection`, to: `22`, tip: ``}]},
  "9": {hd: `The data is stored on the AIMMS Cloud database`, type: 'q', q: `Where exactly is the data stored on your AIMMS Cloud?`, c: [{l: `The data is stored on the Azure Data Lake Storage Gen2`, to: `53`, tip: ``}, {l: `The data is stored on the AIMMS Cloud MySQL database`, to: `54`, tip: ``}]},
  "10": {hd: `The data is stored on an Azure platform and I want a direct connection to the AIMMS Azure platform`, type: 'r', h: `<p>The AIMMS Cloud platform is hosted on the Azure platform. This allows the utilization of Azure-native integration options of which we support the following:</p><ul><li><a href="https://learn.microsoft.com/en-us/azure/virtual-network/virtual-network-peering-overview" rel="noopener noreferrer" target="_blank" title="">VNET-peering</a></li><li><a href="https://community.aimms.com/product-updates/customer-specific-azure-tenant-for-augmenting-your-aimms-apps-with-all-kinds-of-azure-functions-1381" rel="noopener noreferrer" target="_blank" title="">Customer-specific tenant</a> within our Azure environment</li><li>Or other safe ways to establish connectivity between the two environments</li></ul><p>Another option is to consume the Azure REST API&#39;s after possibly setting up the necessary security overlay.</p><p>Each of these options require a custom setup, so please <a href="mailto:support@aimms.com" title="">contact us</a> if you want to explore this further.</p>`},
  "12": {hd: `I am using an external application/ERP that holds the data`, type: 'q', q: `Can the data be exposed through REST API and is it available in Parquet, CSV, XLS, JSON or database format?`, c: [{l: `Yes, it can be exposed through REST API in one of those formats`, to: `13`, tip: ``}, {l: `I want to use external API's to push the data`, to: `30`, tip: ``}, {l: `No, it cannot be exposed through REST API or not in any of those formats`, to: `41`, tip: ``}]},
  "13": {hd: `Yes, it can be exposed through REST API in Parquet, CSV, XLS, JSON or database format`, type: 'q', q: `Once you&#39;ve retrieved your data, how are you going to use and/or store it within the AIMMS Cloud?`, c: [{l: `I am using the retrieved data directly inside my AIMMS model without saving it first`, to: `38`, tip: ``}, {l: `I want to save the data in the Azure Data Lake Storage Gen2 first to use it from there`, to: `39`, tip: ``}, {l: `I want to save the data in the AIMMS MySQL database first to use it from there`, to: `40`, tip: ``}]},
  "20": {hd: `The database is publicly available`, type: 'q', q: `Once you&#39;ve got access to your data, how are you going to use and/or store it within the AIMMS Cloud?`, c: [{l: `I am using the data directly inside my AIMMS model`, to: `42`, tip: ``}, {l: `I'm saving the data in the Azure Data Lake Storage Gen2 first to use it from there`, to: `45`, tip: ``}, {l: `I'm saving the data in the AIMMS MySQL database first to process it from there`, to: `46`, tip: ``}]},
  "21": {hd: `The database can be whitelisted via IP`, type: 'q', content: `<p><a href="mailto:support@aimms.com" title="">Contact us</a> to receive the IP-address of the related AIMMS Cloud that should be whitelisted within your network. After the whitelisting was added successfully, you can access the data directly by using a <a href="https://documentation.aimms.com/functionreference/data-management/database-functions/sqlcreateconnectionstring.html" rel="noopener noreferrer" target="_blank" title="">SQLCreateConnectionString</a>, providing the URL of the database as the ServerName argument.</p>`, q: `Once you&#39;ve got access to your data, how are you going to use and/or store it within the AIMMS Cloud?`, c: [{l: `I am using the retrieved data directly inside my AIMMS model without saving it first`, to: `47`, tip: ``}, {l: `I'm saving the data in the Azure Data Lake Storage Gen2 first to use it from there`, to: `48`, tip: ``}, {l: `I'm saving the data in the AIMMS MySQL database first to use it from there`, to: `49`, tip: ``}]},
  "22": {hd: `We'll need a VPN setup for a direct connection`, type: 'q', q: `Once the VPN is set up, how do you want to use or store the retrieved data?`, c: [{l: `I am using the retrieved data directly inside my AIMMS model without saving it first`, to: `50`, tip: ``}, {l: `I'm saving the data in the Azure Data Lake Storage Gen2 first to use it from there`, to: `51`, tip: ``}, {l: `I'm saving the data in the AIMMS MySQL database first to use it from there`, to: `52`, tip: ``}]},
  "23": {hd: `The data is stored on the Azure Data Lake Storage Gen2 (as part of the AIMMS Cloud)`, type: 'q', q: `Do you want to pull or push the data from the ADLS?`, c: [{l: `I want to pull the data: the target will manage the request`, to: `28`, tip: ``}, {l: `I want to push the data from AIMMS to my target`, to: `29`, tip: ``}]},
  "24": {hd: `The data is only available within the AIMMS model`, type: 'q', q: `Do you want to push the data from AIMMS, or let the target pull it?`, c: [{l: `I want to push the data from AIMMS to my target`, to: `55`, tip: ``}, {l: `I want to pull the data: the target will manage the request`, to: `33`, tip: ``}]},
  "25": {hd: `The data is stored on the AIMMS PRO Storage`, type: 'q', q: `Do you want to push the data from AIMMS, or let the target pull it?`, c: [{l: `I want to push the data from AIMMS to my target`, to: `60`, tip: ``}, {l: `I want to pull the data: the target will manage the request`, to: `61`, tip: ``}]},
  "26": {hd: `The data is stored in the AIMMS Cloud database`, type: 'q', q: `How do you want to expose the data from the AIMMS Cloud database?`, c: [{l: `I want to push the data from AIMMS to my target`, to: `56`, tip: ``}, {l: `I want to pull the data: the target will manage the request`, to: `57`, tip: ``}, {l: `I want to access the data directly from a remote server`, to: `58`, tip: ``}, {l: `I want to access the data directly from my local computer`, to: `59`, tip: ``}]},
  "27": {hd: `I want a direct connection from the AIMMS Azure platform to my Azure platform`, type: 'r', h: `<p>The AIMMS Cloud platform is hosted on the Azure platform. This allows the utilization of Azure-native integration options of which we support the following:</p><ul><li><a href="https://learn.microsoft.com/en-us/azure/virtual-network/virtual-network-peering-overview" rel="noopener noreferrer" target="_blank" title="">VNET-peering</a></li><li><a href="https://community.aimms.com/product-updates/customer-specific-azure-tenant-for-augmenting-your-aimms-apps-with-all-kinds-of-azure-functions-1381" rel="noopener noreferrer" target="_blank" title="">Customer-specific tenant</a> within our Azure environment</li><li>Or other safe ways to establish connectivity between the two environments</li></ul><p>Another option is to consume the Azure REST API&#39;s after possibly setting up the necessary security overlay.</p><p>Each of these options require a custom setup, so please <a href="mailto:support@aimms.com" target="_blank" title="">contact us</a> if you want to explore this further.</p>`},
  "28": {hd: `I want to pull the data: the target will manage the request`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>You want to pull data from your AIMMS Cloud application;</li><li>and you handle JSON, XML, Parquet, database or CSV formatted data.</li></ul><p>In this scenario we offer the option to utilize the <strong>AIMMS Azure Data Lake Storage Gen2 (ADLS)</strong>, and its related Azure-native API&#39;s to exchange the data.&nbsp;</p><p><strong>How does it work?</strong></p><p>You can access the ADLS from any external location by utilizing these two types of Azure API&#39;s:</p><ul><li><a href="https://learn.microsoft.com/en-us/rest/api/storageservices/data-lake-storage-gen2" rel="noopener noreferrer" target="_blank" title="">The Azure Data Lake Storage Gen2 REST API</a> - used for creating and managing so-called filesystems and paths (used for structuring your storage)</li><li><a href="https://learn.microsoft.com/en-us/rest/api/storageservices/blob-service-rest-api" rel="noopener noreferrer" target="_blank" title="">The Azure Blob Storage REST API</a> - used for pulling &#39;blobs&#39; (or: data/files) from the chosen location on the storage. There is a <a href="https://learn.microsoft.com/en-us/rest/api/storageservices/get-blob?tabs=microsoft-entra-id" rel="noopener noreferrer" target="_blank">&#39;GET Blob&#39; request</a> with which you can pull any file from the storage via SAS tokens.</li></ul><p>You can use <a href="https://how-to.aimms.com/Articles/598/598-datalake-tools.html" rel="noopener noreferrer" target="_blank" title="">our DataLake Tools toolkit</a> to help you create the needed SAS-tokens to access your ADLS as part of your AIMMS Cloud subscription. In <a href="https://how-to.aimms.com/Articles/595/595-accessing-the-azure-datalake-externally.html" rel="noopener noreferrer" target="_blank" title="">this how-to article</a> you&#39;ll find an example on how to implement this route.</p><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p>`},
  "29": {hd: `I want to push the data from AIMMS to my target`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>You want to push data from your AIMMS Cloud application to your target;</li><li>and you can PUT the data in JSON, XML, Parquet, database or CSV format via API or alike.</li></ul><p><strong>There are multiple ways to push a file from AIMMS to an external source:</strong></p><ul><li>You can use the Data Exchange Library (DEX) and call a request from an external API to pull the data. Follow this route:<ol><li>Make sure the Data Exchange Library is installed. You will need to use it. If you don&#39;t know how to install a repository library, <a href="https://documentation.aimms.com/general-library/getting-started.html" rel="noopener noreferrer" target="_blank" title="">visit this page</a>.</li><li>Use the <a href="https://documentation.aimms.com/dataexchange/api.html#dex-dls-DownloadFile" rel="noopener noreferrer" target="_blank" title="">dex::dls::DownloadFile</a> to download the data from your ADLS to a (temporary) file.&nbsp;</li><li>Use the Data Exchange Library (DEX) to call the REST API to push the downloaded data file. A function that you will need to use for this is the <a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-NewRequest" rel="noopener noreferrer" target="_blank" title=""><strong>dex::client::NewRequest</strong></a>, in which you can use URLs of the REST API of your source and add additional parameters to make sure you are building the request correctly. An example of this flow can be found in <a href="https://how-to.aimms.com/Articles/533/533-how-to-retrieve-file-data-with-data-exchange.html" rel="noopener noreferrer" target="_blank" title="">this how-to article</a>.</li><li>Use <a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-PerformRequest" rel="noopener noreferrer" target="_blank"><strong>dex::client::PerformRequest</strong></a> to actually perform the request you&#39;ve built.</li><li>And <a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-CloseRequest" rel="noopener noreferrer" target="_blank"><strong>dex::client::CloseRequest</strong></a> to properly close the request you&#39;ve built.</li></ol></li><li>You can use <a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-PutFile" rel="noopener noreferrer" target="_blank" title="">dex::client::PutFile()</a> to put the file directly onto an URL (like Azure Blob Storage or AWS S3).</li><li>You can use <a href="https://documentation.aimms.com/dataexchange/api.html#dex-WriteToDataSource" rel="noopener noreferrer" target="_blank" title="">dex::WriteToDataSource()</a> if you have connected to a database.</li></ul>`},
  "30": {hd: `I want to use external API's to push the data`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>You want to use data in your AIMMS application that is stored in an external application/ERP;</li><li>you can PUSH the data from this source to an external location;</li><li>and you can PUSH the data in JSON, XML, Parquet, database or CSV format.</li></ul><p>In this scenario we offer the option to utilize the <strong>AIMMS Azure Data Lake Storage Gen2 (ADLS)</strong>, and its related Azure-native API&#39;s to exchange the data.&nbsp;</p><p><strong>How does it work?</strong></p><p>The data needs to be pushed to the AIMMS Cloud ADLS so you can use it from there. Azure offers two types of API&#39;s to support this:</p><ul><li><a href="https://learn.microsoft.com/en-us/rest/api/storageservices/data-lake-storage-gen2" rel="noopener noreferrer" target="_blank" title="">The Azure Data Lake Storage Gen2 REST API</a> - used for creating and managing so-called filesystems and paths (used for structuring your storage)</li><li><a href="https://learn.microsoft.com/en-us/rest/api/storageservices/blob-service-rest-api" rel="noopener noreferrer" target="_blank" title="">The Azure Blob Storage REST API</a> - used for exchanging &#39;blobs&#39; (or: data/files) to the chosen location on the storage. There is a <a href="https://learn.microsoft.com/en-us/rest/api/storageservices/put-blob?tabs=azure-ad" rel="noopener noreferrer" target="_blank">&#39;PUT Blob&#39; request</a> with which you can put any file on the storage in any location you have access to via SAS tokens.</li></ul><p>You can use <a href="https://how-to.aimms.com/Articles/598/598-datalake-tools.html" rel="noopener noreferrer" target="_blank" title="">our DataLake Tools toolkit</a> to help you create the needed SAS-tokens to access your ADLS as part of your AIMMS Cloud subscription. In <a href="https://how-to.aimms.com/Articles/595/595-accessing-the-azure-datalake-externally.html" rel="noopener noreferrer" target="_blank" title="">this how-to article</a> you&#39;ll find an example on how to implement this route.</p><p><strong>Processing the data</strong></p><p>Once your data is stored on the Azure Data Lake Storage Gen2 (ADLS), you can use a DEX mapping to <strong>map the data onto your AIMMS model</strong>. This method is part of the <a href="https://documentation.aimms.com/dataexchange/index.html" rel="noopener noreferrer" target="_blank" title="">Data Exchange Library</a>, which needs to be <a href="https://documentation.aimms.com/general-library/getting-started.html" rel="noopener noreferrer" target="_blank" title="">installed</a> before you can use it.&nbsp;</p><ol><li>Use the&nbsp;<a href="https://documentation.aimms.com/dataexchange/api.html#dex-dls-DownloadFile" rel="noopener noreferrer" target="_blank" title="">dex::dls::DownloadFile</a> to download the file you&#39;ve just uploaded to a local directory.&nbsp;<br><br></li><li>To use the data in your AIMMS model, you will need to create a mapping that tells your application how the data in your saved file maps to the AIMMS model and from which file it should be read. There are two ways to generate this mapping:<ol><li><strong><em>automated generation</em></strong> based on your existing model. <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#generate-mapping-file" rel="noopener noreferrer" target="_blank" title="">Read this chapter</a> to learn how to do this. We advise to, especially if you&#39;re doing this for the first time, do this locally (instead of on the cloud) so you can access the generated mappings easily and inspect/edit them when needed. Save the file within your project folder and upload it to the cloud as to be able to use it from there. Make sure to read <a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.</li><li><strong><em>manual generation</em></strong>. You will create the mapping yourself or use an automatically generated mapping and will edit it accordingly. There is a multitude of options available for your mapping. Make sure to read&nbsp;<a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.&nbsp;<br><br></li></ol></li><li>Add the mapping to your model (<a href="https://documentation.aimms.com/dataexchange/api.html#dex-AddMapping" rel="noopener noreferrer" target="_blank" title="">dex::AddMapping</a>) and use <a href="https://documentation.aimms.com/dataexchange/api.html#dex-ReadFromFile" rel="noopener noreferrer" target="_blank" title="">dex::ReadFromFile</a> as described in <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#basic-setup" rel="noopener noreferrer" target="_blank" title="">this chapter of the how-to</a>. The first argument for dex::ReadFromFile will be your downloaded file as specified in step 1. The MappingName will be the mapping name you&#39;ve defined when you created/added the mapping via dex::AddMapping.&nbsp;</li></ol><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. Some common DEX-errors are described on&nbsp;<a href="https://documentation.aimms.com/dataexchange/troubleshoot.html" rel="noopener noreferrer" target="_blank" title="">this page</a>. &nbsp;<br>If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p><p><strong>Further reading/resources/additional information</strong></p><ul><li><a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">On this page</a> you will find a description of the different mapping possibilities for the different supported formats. Read it along <a href="https://documentation.aimms.com/dataexchange/standard.html" rel="noopener noreferrer" target="_blank" title="">with this page</a> to get a full understanding on how the mapping works.</li><li><a href="https://documentation.aimms.com/dataexchange/api.html#methods-for-reading-and-writing-data" rel="noopener noreferrer" target="_blank" title="">Here</a> you can find all the methods you can and should use to generate and use the mapping.</li><li>Find an illustrative example of a mapping <a href="https://how-to.aimms.com/Articles/536/536-name-binds-to.html" rel="noopener noreferrer" target="_blank" title="">on this page</a>.</li></ul>`},
  "33": {hd: `I want to pull the data: the target will manage the request`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>You want to pull data from your AIMMS model;</li><li>and you can handle JSON, XML, Parquet, database or CSV formatted data.</li></ul><p>In this scenario we offer the option to utilize the <strong>AIMMS Azure Data Lake Storage Gen2 (ADLS)</strong>, and its related Azure-native API&#39;s to exchange the data.&nbsp;</p><p><strong>How does it work?</strong></p><p>If you want to pull the data via the target, you need to make the data available via the Azure Data Lake Storage account that is by default part of your cloud. In <a href="https://how-to.aimms.com/Articles/594/594-adls-data-integration.html" rel="noopener noreferrer" target="_blank" title="">this how-to article</a> we describe how to do this.&nbsp;</p><p>You can access the ADLS from any external location by utilizing these two types of Azure API&#39;s:</p><ul><li><a href="https://learn.microsoft.com/en-us/rest/api/storageservices/data-lake-storage-gen2" rel="noopener noreferrer" target="_blank" title="">The Azure Data Lake Storage Gen2 REST API</a> - used for creating and managing so-called filesystems and paths (used for structuring your storage)</li><li><a href="https://learn.microsoft.com/en-us/rest/api/storageservices/blob-service-rest-api" rel="noopener noreferrer" target="_blank" title="">The Azure Blob Storage REST API</a> - used for pulling &#39;blobs&#39; (or: data/files) from the chosen location on the storage. There is a <a href="https://learn.microsoft.com/en-us/rest/api/storageservices/get-blob?tabs=microsoft-entra-id" rel="noopener noreferrer" target="_blank">&#39;GET Blob&#39; request</a> with which you can pull any file from the storage via SAS tokens.</li></ul><p>You can use <a href="https://how-to.aimms.com/Articles/598/598-datalake-tools.html" rel="noopener noreferrer" target="_blank" title="">our DataLake Tools toolkit</a> to help you create the needed SAS-tokens to access your ADLS as part of your AIMMS Cloud subscription. In <a href="https://how-to.aimms.com/Articles/595/595-accessing-the-azure-datalake-externally.html" rel="noopener noreferrer" target="_blank" title="">this how-to article</a> you&#39;ll find an example on how to implement this route.</p><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p>`},
  "38": {hd: `I am using the retrieved data directly inside my AIMMS model without saving it first`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>You want to use data in your AIMMS application that is stored in an external application/ERP/data warehouse or alike;</li><li>this application/ERP has its own REST API and is able to expose the data in JSON, XML, Parquet, database or CSV format;</li><li>and you want to use this data directly in the AIMMS model of the application.&nbsp;</li></ul><p><strong>Your to-do list to implement this route</strong>:</p><ol><li>Make sure the Data Exchange Library is installed. You will need to use it. If you don&#39;t know how to install a repository library,&nbsp;<a href="https://documentation.aimms.com/general-library/getting-started.html" rel="noopener noreferrer" target="_blank" title="">visit this page</a>.<br><br></li><li>Use the Data Exchange Library (DEX) to call the REST API to get the data. A function that you will need to use for this is the <a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-NewRequest" rel="noopener noreferrer" target="_blank" title=""><strong>dex::client::NewRequest</strong></a>, in which you can use URLs of the REST API of your source and add additional parameters to make sure you are building the request correctly. An example of this flow can be found in&nbsp;<a href="https://how-to.aimms.com/Articles/533/533-how-to-retrieve-file-data-with-data-exchange.html" rel="noopener noreferrer" target="_blank" title="">this how-to article</a>.&nbsp;<br><br></li><li>Since you will be using the data directly within the AIMMS model, you will need to fill in the responseFile argument of the dex::client::NewRequest so you can temporarily store the data in either:<ul style="list-style-type: disc;"><li>a (temporary) file. You do not need to create this file before you send out the request. If the file doesn&#39;t exist (yet), it will be created based on the filename specified in this argument.&nbsp;</li><li><a href="https://documentation.aimms.com/dataexchange/api.html#memory-streams" rel="noopener noreferrer" target="_blank" title="">a memory stream</a>, which has the option to be deleted once the request is properly closed (use one # for non-deletion, use ## for deletion).<br><br></li></ul></li><li>Perform the request with <a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-PerformRequest" rel="noopener noreferrer" target="_blank" title="">dex::client::PerformRequest</a>. If your request was processed successfully,&nbsp;<strong>your data is now in the responseFile as specified in the previous step</strong>.&nbsp;<br><br></li><li>To use the data, you will need to create a mapping that tells your application how the data in your responseFile maps to the AIMMS model and from which file it should be read. There are two ways to generate this mapping:<ul><li><strong><em>automated generation</em></strong> based on your existing model. <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#generate-mapping-file" rel="noopener noreferrer" target="_blank" title="">Read this chapter</a> to learn how to do this. We advise to do this locally (instead of on the cloud) so you can access the generated mappings easily and inspect/edit them when needed. Make sure to read <a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.</li><li><strong><em>manual generation</em></strong>. You will create the mapping yourself or use an automatically generated mapping and will edit it accordingly. There is a multitude of options available for your mapping. Make sure to read&nbsp;<a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.<br><br></li></ul></li><li>Add the mapping to your model (<a href="https://documentation.aimms.com/dataexchange/api.html#dex-AddMapping" rel="noopener noreferrer" target="_blank" title="">dex::AddMapping</a>) and use <a href="https://documentation.aimms.com/dataexchange/api.html#dex-ReadFromFile" rel="noopener noreferrer" target="_blank" title="">dex::ReadFromFile</a> as described in&nbsp;<a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#basic-setup" rel="noopener noreferrer" target="_blank" title="">this chapter of the how-to</a>. The first argument for dex::ReadFromFile will be your responseFile argument as defined in step 3. The MappingName will be the mapping name you&#39;ve defined when you created/added the mapping via dex::AddMapping.<br><br></li><li>If all was done successfully, you can now close the request using <a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-CloseRequest" rel="noopener noreferrer" target="_blank" title="">dex::client::CloseRequest</a>. If you&#39;ve used a memory stream with two hashtags (##), it will now automatically be deleted.</li></ol><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. Some common DEX-errors are described on <a href="https://documentation.aimms.com/dataexchange/troubleshoot.html" rel="noopener noreferrer" target="_blank" title="">this page</a>. If you get API errors, make sure you double check the requirements from the REST API you are calling.&nbsp;</p><p>If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" title="">support@aimms.com</a>.</p><p><strong>Further reading/resources/additional information</strong></p><ul><li><a href="https://documentation.aimms.com/dataexchange/rest-client.html" rel="noopener noreferrer" target="_blank" title="">Read more about consuming rest API&#39;s with AIMMS here</a>.&nbsp;</li><li>An overview of methods that can be used for your API calls are <a href="https://documentation.aimms.com/dataexchange/api.html#http-client-methods" rel="noopener noreferrer" target="_blank">here</a>.&nbsp;</li><li>DEX supports authorization through API keys or via <a href="https://documentation.aimms.com/dataexchange/rest-client.html#using-oauth2-for-api-authorization" rel="noopener noreferrer" target="_blank" title="">OAuth2</a> (including <a href="https://documentation.aimms.com/dataexchange/api.html#managing-jwt-tokens" rel="noopener noreferrer" target="_blank" title="">JWT support</a>).&nbsp;</li><li><a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">On this page</a> you will find a description of the different mapping possibilities for the different supported formats. Read it along <a href="https://documentation.aimms.com/dataexchange/standard.html" rel="noopener noreferrer" target="_blank" title="">with this page</a> to get a full understanding on how the mapping works.</li><li><a href="https://documentation.aimms.com/dataexchange/api.html#methods-for-reading-and-writing-data" rel="noopener noreferrer" target="_blank" title="">Here</a> you can find all the methods you can and should use to generate and use the mapping.</li><li>Find an illustrative example of a mapping <a href="https://how-to.aimms.com/Articles/536/536-name-binds-to.html" rel="noopener noreferrer" target="_blank" title="">on this page</a>.</li></ul>`},
  "39": {hd: `I'm saving the data in the Azure Data Lake Storage Gen2 first to use it from there`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>You want to use data in your AIMMS application that is stored in an external application/ERP/data warehouse or alike;</li><li>this source has its own REST API and is able to expose the data in JSON, XML, Parquet, database or CSV format;</li><li>you want to GET the data from this source;</li><li>and you want to save the retrieved data in the AIMMS Azure Data Lake Storage Gen2 (ADLS) to further process it from there.&nbsp;</li></ul><p><strong>Note:</strong> If your external source supports it, using the Azure ADLS-native PUT API directly (the &#34;I want to use external API&#39;s to push the data&#34; option) is the more efficient approach. This route — retrieving data via REST API and then uploading it to ADLS — works, but involves an extra step.</p><p><strong>Your to-do list to implement this route</strong>:</p><ol><li>Make sure the Data Exchange Library is installed. You will need to use it. If you don&#39;t know how to install a repository library,&nbsp;<a href="https://documentation.aimms.com/general-library/getting-started.html" rel="noopener noreferrer" target="_blank" title="">visit this page</a>.<br><br></li><li>Use the Data Exchange Library (DEX) to call the REST API to get the data. A function that you will need to use for this is the <a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-NewRequest" rel="noopener noreferrer" target="_blank" title=""><strong>dex::client::NewRequest</strong></a>, in which you can use URLs of the REST API of your source and add additional parameters to make sure you are building the request correctly. An example of this flow can be found in&nbsp;<a href="https://how-to.aimms.com/Articles/533/533-how-to-retrieve-file-data-with-data-exchange.html" rel="noopener noreferrer" target="_blank" title="">this how-to article</a>.&nbsp;<br><br></li><li>You will need to fill in the responseFile argument of the dex::client::NewRequest so you can (temporarily) store the data in a file. You do not need to create this file before you send out the request. If the file doesn&#39;t exist (yet), it will be created based on the filename specified in this argument.&nbsp;<br><br></li><li>Perform the request with <a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-PerformRequest" rel="noopener noreferrer" target="_blank" title="">dex::client::PerformRequest</a>. If your request was processed successfully,&nbsp;<strong>your data is now in the responseFile as specified in the previous step</strong>.&nbsp;<br><br></li><li>Because you want to store the retrieved data on the ADLS, this may require you to create a file system structure on the ADLS that works for you if you didn&#39;t already implement one. Use our&nbsp;<a href="https://how-to.aimms.com/Articles/598/598-datalake-tools.html" rel="noopener noreferrer" target="_blank" title="">DataLake Tools Toolkit&nbsp;</a>to manage/create file systems.<br><br></li><li>Now you can transfer the responseFile to any location on the ADLS. Use the&nbsp;<a href="https://documentation.aimms.com/dataexchange/api.html#dex-dls-UploadFile" rel="noopener noreferrer" target="_blank" title="">dex::dls::UploadFile</a> method for this.<br><br></li><li>For the following steps, we will assume you are immediately using the retrieved data in your model.&nbsp;<br>If you want to use the data in the saved file in the future, use the&nbsp;<a href="https://documentation.aimms.com/dataexchange/api.html#dex-dls-DownloadFile" rel="noopener noreferrer" target="_blank" title="">dex::dls::DownloadFile</a> to download it to a local directory to process it further.&nbsp;<br><br></li><li>To use the data in your AIMMS model, you will need to create a mapping that tells your application how the data in your saved file maps to the AIMMS model and from which file it should be read. There are two ways to generate this mapping:<ul><li><strong><em>automated generation</em></strong> based on your existing model. <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#generate-mapping-file" rel="noopener noreferrer" target="_blank" title="">Read this chapter</a> to learn how to do this. We advise to, especially if you&#39;re doing this for the first time, do this locally (instead of on the cloud) so you can access the generated mappings easily and inspect/edit them when needed. Save the file within your project folder and upload it to the cloud as to be able to use it from there. Make sure to read <a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.</li><li><strong><em>manual generation</em></strong>. You will create the mapping yourself or use an automatically generated mapping and will edit it accordingly. There is a multitude of options available for your mapping. Make sure to read&nbsp;<a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.&nbsp;<br><br></li></ul></li><li>Add the mapping to your model (<a href="https://documentation.aimms.com/dataexchange/api.html#dex-AddMapping" rel="noopener noreferrer" target="_blank" title="">dex::AddMapping</a>) and use <a href="https://documentation.aimms.com/dataexchange/api.html#dex-ReadFromFile" rel="noopener noreferrer" target="_blank" title="">dex::ReadFromFile</a> as described in&nbsp;<a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#basic-setup" rel="noopener noreferrer" target="_blank" title="">this chapter of the how-to</a>. The first argument for dex::ReadFromFile will be your responseFile argument as used in step 3. The MappingName will be the mapping name you&#39;ve defined when you created/added the mapping via dex::AddMapping.&nbsp;<br><strong><em>!NOTE that we are directly using the responseFile as retrieved via the API, and NOT the file as saved on the ADLS. This would require additional steps and there is no benefit to it unless you&#39;d want to edit the data in between saving and using it. For this route, we assume this is not the case.</em></strong><br><br></li><li>If all was done successfully, you can now close the request using <a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-CloseRequest" rel="noopener noreferrer" target="_blank" title="">dex::client::CloseRequest</a>. The responseFile argument will no longer be available, but the file is saved on the ADLS so you can use it from there in the future by using the <a href="https://documentation.aimms.com/dataexchange/api.html#dex-dls-DownloadFile" rel="noopener noreferrer" target="_blank" title="">dex::dls::DownloadFile</a> method.</li></ol><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. Some common DEX-errors are described on <a href="https://documentation.aimms.com/dataexchange/troubleshoot.html" rel="noopener noreferrer" target="_blank" title="">this page</a>. If you get API errors, make sure you double check the requirements from the REST API you are calling.&nbsp;</p><p>If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p><p><strong>Further reading/resources/additional information</strong></p><ul><li><a href="https://documentation.aimms.com/dataexchange/rest-client.html" rel="noopener noreferrer" target="_blank" title="">Read more about consuming rest API&#39;s with AIMMS here</a>.&nbsp;</li><li>An overview of methods that can be used for your API calls are <a href="https://documentation.aimms.com/dataexchange/api.html#http-client-methods" rel="noopener noreferrer" target="_blank">here</a>.&nbsp;</li><li>DEX supports authorization through API keys or via <a href="https://documentation.aimms.com/dataexchange/rest-client.html#using-oauth2-for-api-authorization" rel="noopener noreferrer" target="_blank" title="">OAuth2</a> (including <a href="https://documentation.aimms.com/dataexchange/api.html#managing-jwt-tokens" rel="noopener noreferrer" target="_blank" title="">JWT support</a>).&nbsp;</li><li><a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">On this page</a> you will find a description of the different mapping possibilities for the different supported formats. Read it along <a href="https://documentation.aimms.com/dataexchange/standard.html" rel="noopener noreferrer" target="_blank" title="">with this page</a> to get a full understanding on how the mapping works.</li><li><a href="https://documentation.aimms.com/dataexchange/api.html#methods-for-reading-and-writing-data" rel="noopener noreferrer" target="_blank" title="">Here</a> you can find all the methods you can and should use to generate and use the mapping.</li><li>Find an illustrative example of a mapping <a href="https://how-to.aimms.com/Articles/536/536-name-binds-to.html" rel="noopener noreferrer" target="_blank" title="">on this page</a>.</li></ul>`},
  "40": {hd: `I'm saving the data in the AIMMS MySQL database first to use it from there`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>You want to use data in your AIMMS application that is stored in an external application/ERP/data warehouse or alike;</li><li>this source has its own REST API and is able to expose the data in JSON, XML, Parquet, database or CSV format;</li><li>you want to GET the data from this source;</li><li>and you want to save the retrieved data in the AIMMS Cloud MySQL database to further process it from there.&nbsp;</li></ul><p><strong>Your to-do list to implement this route</strong>:</p><ol><li>Make sure the Data Exchange Library is installed. You will need to use it. If you don&#39;t know how to install a repository library,&nbsp;<a href="https://documentation.aimms.com/general-library/getting-started.html" rel="noopener noreferrer" target="_blank" title="">visit this page</a>.<br><br></li><li>Double check if your Cloud setup <a href="https://how-to.aimms.com/Articles/596/596-mysql-db-cloud.html" rel="noopener noreferrer" target="_blank" title="">has a MySQL database attached to it</a>. If you are in doubt, contact us at&nbsp;<a href="mailto:support@aimms.com" title="">support@aimms.com</a>.<br><br></li><li>Use the Data Exchange Library (DEX) to call the REST API to get the data. A function that you will need to use for this is the <a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-NewRequest" rel="noopener noreferrer" target="_blank" title=""><strong>dex::client::NewRequest</strong></a>, in which you can use URLs of the REST API of your source and add additional parameters to make sure you are building the request correctly. An example of this flow can be found in&nbsp;<a href="https://how-to.aimms.com/Articles/533/533-how-to-retrieve-file-data-with-data-exchange.html" rel="noopener noreferrer" target="_blank" title="">this how-to article</a>.&nbsp;<br><br></li><li>You will need to fill in the responseFile argument of the dex::client::NewRequest so you can (temporarily) store the data in either:<ul><li>a (temporary) file. You do not need to create this file before you send out the request. If the file doesn&#39;t exist (yet), it will be created based on the filename specified in this argument.&nbsp;</li><li><a href="https://documentation.aimms.com/dataexchange/api.html#memory-streams" rel="noopener noreferrer" target="_blank" title="">a memory stream</a>, which has the option to be deleted once the request is properly closed (use one # for non-deletion, use ## for deletion).<br><br></li></ul></li><li>Perform the request with <a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-PerformRequest" rel="noopener noreferrer" target="_blank" title="">dex::client::PerformRequest</a>. If your request was processed successfully,&nbsp;<strong>your data is now in the responseFile as specified in the previous step</strong>.&nbsp;<br><br></li><li>To save the data on the MySQL Cloud database you should use the <a href="https://documentation.aimms.com/functionreference/data-management/database-functions/sqlcreateconnectionstring.html#SQLCreateConnectionString" rel="noopener noreferrer" target="_blank" title="">SQLCreateConnectionString</a> method to create a connection to your database from within your AIMMS application. The URL to your database (needed for the ServerName argument) on the cloud is always [yourcloudname].db.aimms.cloud. Once the connection has been established, you can use the&nbsp;<a href="https://documentation.aimms.com/language-reference/data-communication-components/communicating-with-databases/index.html" rel="noopener noreferrer" target="_blank" title="">AIMMS Database functions</a> to write.<br><br></li><li>To use the data from the database in your AIMMS model, you have two options:&nbsp;<ul><li>use the <a href="https://documentation.aimms.com/language-reference/data-communication-components/communicating-with-databases/index.html" rel="noopener noreferrer" target="_blank" title="">AIMMS Database functions</a> for reading.</li><li>create a DEX mapping that tells your application how the data in your database maps to the AIMMS model and from which database/tables it should be read. There are two ways to generate this mapping:<ul><li><strong><em>automated generation</em></strong> based on your existing model. <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#generate-mapping-file" rel="noopener noreferrer" target="_blank" title="">Read this chapter</a> to learn how to do this. We advise to do this locally (instead of on the cloud) so you can access the generated mappings easily and inspect/edit them when needed. Make sure to read <a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.</li><li><strong><em>manual generation</em></strong>. You will create the mapping yourself or use an automatically generated mapping and will edit it accordingly. There is a multitude of options available for your mapping. Make sure to read&nbsp;<a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.<br><br></li></ul></li></ul></li><li>[<em>only applicable if you are using a DEX mapping</em>] Add the mapping to your model (<a href="https://documentation.aimms.com/dataexchange/api.html#dex-AddMapping" rel="noopener noreferrer" target="_blank" title="">dex::AddMapping</a>) and use <a href="https://documentation.aimms.com/dataexchange/api.html#dex-ReadFromFile" rel="noopener noreferrer" target="_blank" title="">dex::ReadFromFile</a> as described in&nbsp;<a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#basic-setup" rel="noopener noreferrer" target="_blank" title="">this chapter of the how-to</a>. The first argument for dex::ReadFromFile will be your responseFile argument as defined in step 3. The MappingName will be the mapping name you&#39;ve defined when you created/added the mapping via dex::AddMapping.<br><br></li><li>If all was done successfully, you can now close the request using <a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-CloseRequest" rel="noopener noreferrer" target="_blank" title="">dex::client::CloseRequest</a>. If you&#39;ve used a memory stream with two hashtags (##), it will now automatically be deleted.</li></ol><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. Some common DEX-errors are described on <a href="https://documentation.aimms.com/dataexchange/troubleshoot.html" rel="noopener noreferrer" target="_blank" title="">this page</a>. &nbsp;</p><p>If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p><p><strong>Further reading/resources/additional information</strong></p><ul><li><a href="https://documentation.aimms.com/dataexchange/rest-client.html" rel="noopener noreferrer" target="_blank" title="">Read more about consuming rest API&#39;s with AIMMS here</a>.&nbsp;</li><li>An overview of methods that can be used for your API calls are <a href="https://documentation.aimms.com/dataexchange/api.html#http-client-methods" rel="noopener noreferrer" target="_blank">here</a>.&nbsp;</li><li>DEX supports authorization through API keys or via <a href="https://documentation.aimms.com/dataexchange/rest-client.html#using-oauth2-for-api-authorization" rel="noopener noreferrer" target="_blank" title="">OAuth2</a> (including <a href="https://documentation.aimms.com/dataexchange/api.html#managing-jwt-tokens" rel="noopener noreferrer" target="_blank" title="">JWT support</a>).&nbsp;</li><li><a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">On this page</a> you will find a description of the different mapping possibilities for the different supported formats. Read it along <a href="https://documentation.aimms.com/dataexchange/standard.html" rel="noopener noreferrer" target="_blank" title="">with this page</a> to get a full understanding on how the mapping works.</li><li><a href="https://documentation.aimms.com/dataexchange/api.html#methods-for-reading-and-writing-data" rel="noopener noreferrer" target="_blank" title="">Here</a> you can find all the methods you can and should use to generate and use the mapping.</li><li>Find an illustrative example of a mapping <a href="https://how-to.aimms.com/Articles/536/536-name-binds-to.html" rel="noopener noreferrer" target="_blank" title="">on this page</a>.</li></ul>`},
  "41": {hd: `No, it cannot be exposed through REST API or not in any of those formats`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>You want to use data in your AIMMS application that is stored in an external application/ERP;</li><li>the data cannot be exposed or retrieved in JSON, XML, Excel, Parquet, database or CSV format.</li></ul><p>In this scenario the alternative is to export the data manually to any of the supported file formats and then PUSH that file through API (as described in the other route) or use the AIMMS WebUI to upload the file manually. Take a look at the <a href="https://documentation.aimms.com/webui/upload-widget.html" rel="noopener noreferrer" target="_blank" title="">WebUI Upload Widget</a>, which you will probably want to combine with <a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">a mapping</a> (as made available by the Data Exchange Library) to map the data correctly onto your AIMMS model. A full example can be found <a href="https://how-to.aimms.com/Articles/383/383-contract-allocation.html" rel="noopener noreferrer" target="_blank" title="">here</a>.</p><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. Some common DEX-errors are described on&nbsp;<a href="https://documentation.aimms.com/dataexchange/troubleshoot.html" rel="noopener noreferrer" target="_blank" title="">this page</a>. &nbsp;<br>If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p><p><strong>Further reading/resources/additional information</strong></p><ul><li><a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">On this page</a> you will find a description of the different mapping possibilities for the different supported formats. Read it along <a href="https://documentation.aimms.com/dataexchange/standard.html" rel="noopener noreferrer" target="_blank" title="">with this page</a> to get a full understanding on how the mapping works.</li><li><a href="https://documentation.aimms.com/dataexchange/api.html#methods-for-reading-and-writing-data" rel="noopener noreferrer" target="_blank" title="">Here</a> you can find all the methods you can and should use to generate and use the mapping.</li><li>Find an illustrative example of a mapping <a href="https://how-to.aimms.com/Articles/536/536-name-binds-to.html" rel="noopener noreferrer" target="_blank" title="">on this page</a>.</li></ul>`},
  "42": {hd: `I am using the data directly inside my AIMMS model`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>Your data is stored in a database that you have access to;</li><li>you want to access this data directly from your AIMMS model.</li></ul><p><strong>Your to-do list to implement this route</strong>:</p><ol><li>Use the&nbsp;<a href="https://documentation.aimms.com/functionreference/data-management/database-functions/sqlcreateconnectionstring.html#SQLCreateConnectionString" rel="noopener noreferrer" target="_blank" title="">SQLCreateConnectionString</a> method to create a connection to your database from within your AIMMS application. The URL to your database is needed for the ServerName argument.&nbsp;<br>[note: if you are using the MySQL database on the cloud, the direct URL to the database is always [yourcloudname].db.aimms.cloud]<br><br></li><li>To use the data from the database in your AIMMS model, you have two options:&nbsp;<ul><li>use the <a href="https://documentation.aimms.com/language-reference/data-communication-components/communicating-with-databases/index.html" rel="noopener noreferrer" target="_blank" title="">AIMMS Database functions</a> for reading.</li><li>create a DEX mapping that tells your application how the data in your database maps to the AIMMS model and from which database/tables it should be read. Read <a href="https://documentation.aimms.com/dataexchange/sqldb.html" rel="noopener noreferrer" target="_blank" title="">this page</a> to learn more.</li></ul></li></ol><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. Some common DEX-errors are described on&nbsp;<a href="https://documentation.aimms.com/dataexchange/troubleshoot.html" rel="noopener noreferrer" target="_blank" title="">this page</a>. &nbsp;<br>If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p>`},
  "45": {hd: `I'm saving the data in the Azure Data Lake Storage Gen2 first to use it from there`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>Your data is stored in a database that you have access to;</li><li>you want to save the data on the Azure Data Lake Storage Gen2 (ADLS) before further processing it.</li></ul><p><strong>Your to-do list to implement this route</strong>:</p><ol><li>Use the&nbsp;<a href="https://documentation.aimms.com/functionreference/data-management/database-functions/sqlcreateconnectionstring.html#SQLCreateConnectionString" rel="noopener noreferrer" target="_blank" title="">SQLCreateConnectionString</a> method to create a connection to your database from within your AIMMS application. The URL to your database is needed for the ServerName argument.&nbsp;<br>[note: if you are using the MySQL database on the cloud, the direct URL to the database is always [yourcloudname].db.aimms.cloud]<br><br></li><li>You will need to save the data from your database into a file that can be saved on the ADLS. Supported file formats are Parquet, CSV, JSON, XML and Excel.&nbsp;<br><br></li><li>Because you want to store the data on the ADLS, this may require you to create a file system structure on the ADLS that works for you if you didn&#39;t already implement one. Use our&nbsp;<a href="https://how-to.aimms.com/Articles/598/598-datalake-tools.html" rel="noopener noreferrer" target="_blank" title="">DataLake Tools Toolkit&nbsp;</a>to manage/create file systems.<br><br></li><li>Now you can transfer the created file to any location on the ADLS. Use the&nbsp;<a href="https://documentation.aimms.com/dataexchange/api.html#dex-dls-UploadFile" rel="noopener noreferrer" target="_blank" title="">dex::dls::UploadFile</a> method for this.<br><br></li><li>For the following steps, we will assume you are immediately using the retrieved data in your model.&nbsp;<br>If you want to use the data in the saved file in the future, use the&nbsp;<a href="https://documentation.aimms.com/dataexchange/api.html#dex-dls-DownloadFile" rel="noopener noreferrer" target="_blank" title="">dex::dls::DownloadFile</a> to download it to a local directory to process it further.&nbsp;<br><br></li><li>To use the data in your AIMMS model, you will need to create a mapping that tells your application how the data in your saved file maps to the AIMMS model and from which file it should be read. There are two ways to generate this mapping:<ul><li><strong><em>automated generation</em></strong> based on your existing model. <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#generate-mapping-file" rel="noopener noreferrer" target="_blank" title="">Read this chapter</a> to learn how to do this. We advise to, especially if you&#39;re doing this for the first time, do this locally (instead of on the cloud) so you can access the generated mappings easily and inspect/edit them when needed. Save the file within your project folder and upload it to the cloud as to be able to use it from there. Make sure to read <a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.</li><li><strong><em>manual generation</em></strong>. You will create the mapping yourself or use an automatically generated mapping and will edit it accordingly. There is a multitude of options available for your mapping. Make sure to read&nbsp;<a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.&nbsp;<br><br></li></ul></li><li>Add the mapping to your model (<a href="https://documentation.aimms.com/dataexchange/api.html#dex-AddMapping" rel="noopener noreferrer" target="_blank" title="">dex::AddMapping</a>) and use <a href="https://documentation.aimms.com/dataexchange/api.html#dex-ReadFromFile" rel="noopener noreferrer" target="_blank" title="">dex::ReadFromFile</a> as described in&nbsp;<a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#basic-setup" rel="noopener noreferrer" target="_blank" title="">this chapter of the how-to</a>. The first argument for dex::ReadFromFile will be the file you specified in step 2. The MappingName will be the mapping name you&#39;ve defined when you created/added the mapping via dex::AddMapping.&nbsp;<br><strong><em>!NOTE that we are directly using the responseFile as retrieved via the API, and NOT the file as saved on the ADLS. This would require additional steps and there is no benefit to it unless you&#39;d want to edit the data in between saving and using it. For this route, we assume this is not the case.</em></strong></li></ol><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. Some common DEX-errors are described on&nbsp;<a href="https://documentation.aimms.com/dataexchange/troubleshoot.html" rel="noopener noreferrer" target="_blank" title="">this page</a>. &nbsp;<br>If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p>`},
  "46": {hd: `I'm saving the data in the AIMMS MySQL database first to process it from there`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>Your data is stored in a database that you have access to;</li><li>and you want to save the data in the AIMMS Cloud MySQL database to further process it from there.&nbsp;</li></ul><p><strong>Your to-do list to implement this route</strong>:</p><ol><li>Make sure the Data Exchange Library is installed. You will need to use it. If you don&#39;t know how to install a repository library,&nbsp;<a href="https://documentation.aimms.com/general-library/getting-started.html" rel="noopener noreferrer" target="_blank" title="">visit this page</a>.<br><br></li><li>Double check if your Cloud setup <a href="https://how-to.aimms.com/Articles/596/596-mysql-db-cloud.html" rel="noopener noreferrer" target="_blank" title="">has a MySQL database attached to it</a>. If you are in doubt, contact us at&nbsp;<a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.<br><br></li><li>Use the&nbsp;<a href="https://documentation.aimms.com/functionreference/data-management/database-functions/sqlcreateconnectionstring.html#SQLCreateConnectionString" rel="noopener noreferrer" target="_blank" title="">SQLCreateConnectionString</a> method to create a connection to your database from within your AIMMS application. The URL to your database is needed for the ServerName argument.&nbsp;<br>[note: if you are using the MySQL database on the cloud, the direct URL to the database is always [yourcloudname].db.aimms.cloud]<br><br></li><li>You will need to read the relevant data from the database into AIMMS identifiers within your model as to be able to save them in the MySQL Cloud database. For this, you have two options:&nbsp;<ul><li>use the <a href="https://documentation.aimms.com/language-reference/data-communication-components/communicating-with-databases/index.html" rel="noopener noreferrer" target="_blank" title="">AIMMS Database functions</a> for reading.</li><li>create a DEX mapping that tells your application how the data in your database maps to the AIMMS model and from which database/tables it should be read. Read&nbsp;<a href="https://documentation.aimms.com/dataexchange/sqldb.html" rel="noopener noreferrer" target="_blank" title="">this page</a> to learn more.<br><br></li></ul></li><li>To save the data on the MySQL Cloud database you should use the <a href="https://documentation.aimms.com/functionreference/data-management/database-functions/sqlcreateconnectionstring.html#SQLCreateConnectionString" rel="noopener noreferrer" target="_blank" title="">SQLCreateConnectionString</a> method to create a connection to your database from within your AIMMS application. The URL to your database (needed for the ServerName argument) on the cloud is always [yourcloudname].db.aimms.cloud. Once the connection has been established, you can use the&nbsp;<a href="https://documentation.aimms.com/language-reference/data-communication-components/communicating-with-databases/index.html" rel="noopener noreferrer" target="_blank" title="">AIMMS Database functions</a> to write.<br><br></li><li>To use the data from the database in your AIMMS model, you have two options:&nbsp;<ul><li>use the <a href="https://documentation.aimms.com/language-reference/data-communication-components/communicating-with-databases/index.html" rel="noopener noreferrer" target="_blank" title="">AIMMS Database functions</a> for reading.</li><li>create a DEX mapping that tells your application how the data in your database maps to the AIMMS model and from which database/tables it should be read. There are two ways to generate this mapping:<ul><li><strong><em>automated generation</em></strong> based on your existing model. <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#generate-mapping-file" rel="noopener noreferrer" target="_blank" title="">Read this chapter</a> to learn how to do this. We advise to do this locally (instead of on the cloud) so you can access the generated mappings easily and inspect/edit them when needed. Make sure to read <a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.</li><li><strong><em>manual generation</em></strong>. You will create the mapping yourself or use an automatically generated mapping and will edit it accordingly. There is a multitude of options available for your mapping. Make sure to read&nbsp;<a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.<br><br></li></ul></li></ul></li><li>[<em>only applicable if you are using a DEX mapping</em>] Add the mapping to your model (<a href="https://documentation.aimms.com/dataexchange/api.html#dex-AddMapping" rel="noopener noreferrer" target="_blank" title="">dex::AddMapping</a>) and use <a href="https://documentation.aimms.com/dataexchange/api.html#dex-ReadFromFile" rel="noopener noreferrer" target="_blank" title="">dex::ReadFromFile</a> as described in <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#basic-setup" rel="noopener noreferrer" target="_blank" title="">this chapter of the how-to</a>. The first argument for dex::ReadFromFile will be the file you saved in the previous step. The MappingName will be the mapping name you&#39;ve defined when you created/added the mapping via dex::AddMapping.</li></ol><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. Some common DEX-errors are described on <a href="https://documentation.aimms.com/dataexchange/troubleshoot.html" rel="noopener noreferrer" target="_blank" title="">this page</a>. &nbsp;</p><p>If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p><p><strong>Further reading/resources/additional information</strong></p><ul><li><a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">On this page</a> you will find a description of the different mapping possibilities for the different supported formats. Read it along <a href="https://documentation.aimms.com/dataexchange/standard.html" rel="noopener noreferrer" target="_blank" title="">with this page</a> to get a full understanding on how the mapping works.</li><li><a href="https://documentation.aimms.com/dataexchange/api.html#methods-for-reading-and-writing-data" rel="noopener noreferrer" target="_blank" title="">Here</a> you can find all the methods you can and should use to generate and use the mapping.</li><li>Find an illustrative example of a mapping <a href="https://how-to.aimms.com/Articles/536/536-name-binds-to.html" rel="noopener noreferrer" target="_blank" title="">on this page</a>.</li><li>Read more about the AIMMS Cloud MySQL database <a href="https://documentation.aimms.com/cloud/db-config.html" rel="noopener noreferrer" target="_blank" title="">here</a>.</li></ul>`},
  "47": {hd: `I am using the retrieved data directly inside my AIMMS model without saving it first`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>Your data is stored in a database that&#39;s within a secured network and can be accessed from whitelisted IP&#39;s;</li><li>you want to access this data directly from your AIMMS model.</li></ul><p><strong>Your to-do list to implement this route</strong>:</p><ol><li><a href="mailto:support@aimms.com" target="_blank" title="">Contact us</a> to receive the IP-address of the related AIMMS Cloud that should be whitelisted within your network.<br><br></li><li>After the IP has been whitelisted, use the&nbsp;<a href="https://documentation.aimms.com/functionreference/data-management/database-functions/sqlcreateconnectionstring.html#SQLCreateConnectionString" rel="noopener noreferrer" target="_blank" title="">SQLCreateConnectionString</a> method to create a connection to your database from within your AIMMS application. The URL to your database is needed for the ServerName argument.&nbsp;<br>[note: if you are using the MySQL database on the cloud, the direct URL to the database is always [yourcloudname].db.aimms.cloud]<br><br></li><li>To use the data from the database in your AIMMS model, you have two options:&nbsp;<ul><li>use the <a href="https://documentation.aimms.com/language-reference/data-communication-components/communicating-with-databases/index.html" rel="noopener noreferrer" target="_blank" title="">AIMMS Database functions</a> for reading.</li><li>create a DEX mapping that tells your application how the data in your database maps to the AIMMS model and from which database/tables it should be read. Read <a href="https://documentation.aimms.com/dataexchange/sqldb.html" rel="noopener noreferrer" target="_blank" title="">this page</a> to learn more.</li></ul></li></ol><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. Some common DEX-errors are described on&nbsp;<a href="https://documentation.aimms.com/dataexchange/troubleshoot.html" rel="noopener noreferrer" target="_blank" title="">this page</a>. &nbsp;<br>If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p>`},
  "48": {hd: `I'm saving the data in the Azure Data Lake Storage Gen2 first to use it from there`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>Your data is stored in a database that&#39;s within a secured network and can be accessed from whitelisted IP&#39;s;</li><li>you want to save the data on the Azure Data Lake Storage Gen2 (ADLS) before further processing it.</li></ul><p><strong>Your to-do list to implement this route</strong>:</p><ol><li><a href="mailto:support@aimms.com" target="_blank" title="">Contact us</a> to receive the IP-address of the related AIMMS Cloud that should be whitelisted within your network.<br><br></li><li>After the IP has been whitelisted, use the&nbsp;<a href="https://documentation.aimms.com/functionreference/data-management/database-functions/sqlcreateconnectionstring.html#SQLCreateConnectionString" rel="noopener noreferrer" target="_blank" title="">SQLCreateConnectionString</a> method to create a connection to your database from within your AIMMS application. The URL to your database is needed for the ServerName argument.&nbsp;<br>[note: if you are using the MySQL database on the cloud, the direct URL to the database is always [yourcloudname].db.aimms.cloud]</li><li>You will need to save the data from your database into a file that can be saved on the ADLS. Supported file formats are Parquet, CSV, JSON, XML and Excel.&nbsp;<br><br></li><li>Because you want to store the data on the ADLS, this may require you to create a file system structure on the ADLS that works for you if you didn&#39;t already implement one. Use our&nbsp;<a href="https://how-to.aimms.com/Articles/598/598-datalake-tools.html" rel="noopener noreferrer" target="_blank" title="">DataLake Tools Toolkit&nbsp;</a>to manage/create file systems.<br><br></li><li>Now you can transfer the created file to any location on the ADLS. Use the&nbsp;<a href="https://documentation.aimms.com/dataexchange/api.html#dex-dls-UploadFile" rel="noopener noreferrer" target="_blank" title="">dex::dls::UploadFile</a> method for this.<br><br></li><li>For the following steps, we will assume you are immediately using the retrieved data in your model.&nbsp;<br>If you want to use the data in the saved file in the future, use the&nbsp;<a href="https://documentation.aimms.com/dataexchange/api.html#dex-dls-DownloadFile" rel="noopener noreferrer" target="_blank" title="">dex::dls::DownloadFile</a> to download it to a local directory to process it further.&nbsp;<br><br></li><li>To use the data in your AIMMS model, you will need to create a mapping that tells your application how the data in your saved file maps to the AIMMS model and from which file it should be read. There are two ways to generate this mapping:<ul><li><strong><em>automated generation</em></strong> based on your existing model. <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#generate-mapping-file" rel="noopener noreferrer" target="_blank" title="">Read this chapter</a> to learn how to do this. We advise to, especially if you&#39;re doing this for the first time, do this locally (instead of on the cloud) so you can access the generated mappings easily and inspect/edit them when needed. Save the file within your project folder and upload it to the cloud as to be able to use it from there. Make sure to read <a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.</li><li><strong><em>manual generation</em></strong>. You will create the mapping yourself or use an automatically generated mapping and will edit it accordingly. There is a multitude of options available for your mapping. Make sure to read&nbsp;<a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.&nbsp;<br><br></li></ul></li><li>Add the mapping to your model (<a href="https://documentation.aimms.com/dataexchange/api.html#dex-AddMapping" rel="noopener noreferrer" target="_blank" title="">dex::AddMapping</a>) and use <a href="https://documentation.aimms.com/dataexchange/api.html#dex-ReadFromFile" rel="noopener noreferrer" target="_blank" title="">dex::ReadFromFile</a> as described in&nbsp;<a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#basic-setup" rel="noopener noreferrer" target="_blank" title="">this chapter of the how-to</a>. The first argument for dex::ReadFromFile will be the file you specified in step 2. The MappingName will be the mapping name you&#39;ve defined when you created/added the mapping via dex::AddMapping.&nbsp;</li></ol><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. Some common DEX-errors are described on&nbsp;<a href="https://documentation.aimms.com/dataexchange/troubleshoot.html" rel="noopener noreferrer" target="_blank" title="">this page</a>. &nbsp;<br>If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p>`},
  "49": {hd: `I'm saving the data in the AIMMS MySQL database first to use it from there`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>Your data is stored in a database that&#39;s within a secured network and can be accessed from whitelisted IP&#39;s;</li><li>and you want to save the data in the AIMMS Cloud MySQL database to further process it from there.&nbsp;</li></ul><p><strong>Your to-do list to implement this route</strong>:</p><ol><li>Make sure the Data Exchange Library is installed. You will need to use it. If you don&#39;t know how to install a repository library,&nbsp;<a href="https://documentation.aimms.com/general-library/getting-started.html" rel="noopener noreferrer" target="_blank" title="">visit this page</a>.<br><br></li><li>Double check if your Cloud setup <a href="https://how-to.aimms.com/Articles/596/596-mysql-db-cloud.html" rel="noopener noreferrer" target="_blank" title="">has a MySQL database attached to it</a>. If you are in doubt, contact us at&nbsp;<a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.<br><br></li><li><a href="mailto:support@aimms.com" target="_blank" title="">Contact us</a> to receive the IP-address of the related AIMMS Cloud that should be whitelisted within your network.<br><br></li><li>After the IP has been whitelisted, use the&nbsp;<a href="https://documentation.aimms.com/functionreference/data-management/database-functions/sqlcreateconnectionstring.html#SQLCreateConnectionString" rel="noopener noreferrer" target="_blank" title="">SQLCreateConnectionString</a> method to create a connection to your database from within your AIMMS application. The URL to your database is needed for the ServerName argument.&nbsp;<br>[note: if you are using the MySQL database on the cloud, the direct URL to the database is always [yourcloudname].db.aimms.cloud]<br><br></li><li>You will need to read the relevant data from the database into AIMMS identifiers within your model as to be able to save them in the MySQL Cloud database. For this, you have two options:&nbsp;<ul><li>use the <a href="https://documentation.aimms.com/language-reference/data-communication-components/communicating-with-databases/index.html" rel="noopener noreferrer" target="_blank" title="">AIMMS Database functions</a> for reading.</li><li>create a DEX mapping that tells your application how the data in your database maps to the AIMMS model and from which database/tables it should be read. Read&nbsp;<a href="https://documentation.aimms.com/dataexchange/sqldb.html" rel="noopener noreferrer" target="_blank" title="">this page</a> to learn more.<br><br></li></ul></li><li>To save the data on the MySQL Cloud database you should use the <a href="https://documentation.aimms.com/functionreference/data-management/database-functions/sqlcreateconnectionstring.html#SQLCreateConnectionString" rel="noopener noreferrer" target="_blank" title="">SQLCreateConnectionString</a> method to create a connection to your database from within your AIMMS application. The URL to your database (needed for the ServerName argument) on the cloud is always [yourcloudname].db.aimms.cloud. Once the connection has been established, you can use the&nbsp;<a href="https://documentation.aimms.com/language-reference/data-communication-components/communicating-with-databases/index.html" rel="noopener noreferrer" target="_blank" title="">AIMMS Database functions</a> to write.<br><br></li><li>To use the data from the database in your AIMMS model, you have two options:&nbsp;<ul><li>use the <a href="https://documentation.aimms.com/language-reference/data-communication-components/communicating-with-databases/index.html" rel="noopener noreferrer" target="_blank" title="">AIMMS Database functions</a> for reading.</li><li>create a DEX mapping that tells your application how the data in your database maps to the AIMMS model and from which database/tables it should be read. There are two ways to generate this mapping:<ul><li><strong><em>automated generation</em></strong> based on your existing model. <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#generate-mapping-file" rel="noopener noreferrer" target="_blank" title="">Read this chapter</a> to learn how to do this. We advise to do this locally (instead of on the cloud) so you can access the generated mappings easily and inspect/edit them when needed. Make sure to read <a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.</li><li><strong><em>manual generation</em></strong>. You will create the mapping yourself or use an automatically generated mapping and will edit it accordingly. There is a multitude of options available for your mapping. Make sure to read&nbsp;<a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.<br><br></li></ul></li></ul></li><li>[<em>only applicable if you are using a DEX mapping</em>] Add the mapping to your model (<a href="https://documentation.aimms.com/dataexchange/api.html#dex-AddMapping" rel="noopener noreferrer" target="_blank" title="">dex::AddMapping</a>) and use <a href="https://documentation.aimms.com/dataexchange/api.html#dex-ReadFromFile" rel="noopener noreferrer" target="_blank" title="">dex::ReadFromFile</a> as described in <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#basic-setup" rel="noopener noreferrer" target="_blank" title="">this chapter of the how-to</a>. The first argument for dex::ReadFromFile will be the file you saved in the previous step. The MappingName will be the mapping name you&#39;ve defined when you created/added the mapping via dex::AddMapping.</li></ol><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. Some common DEX-errors are described on <a href="https://documentation.aimms.com/dataexchange/troubleshoot.html" rel="noopener noreferrer" target="_blank" title="">this page</a>. &nbsp;</p><p>If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p><p><strong>Further reading/resources/additional information</strong></p><ul><li><a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">On this page</a> you will find a description of the different mapping possibilities for the different supported formats. Read it along <a href="https://documentation.aimms.com/dataexchange/standard.html" rel="noopener noreferrer" target="_blank" title="">with this page</a> to get a full understanding on how the mapping works.</li><li><a href="https://documentation.aimms.com/dataexchange/api.html#methods-for-reading-and-writing-data" rel="noopener noreferrer" target="_blank" title="">Here</a> you can find all the methods you can and should use to generate and use the mapping.</li><li>Find an illustrative example of a mapping <a href="https://how-to.aimms.com/Articles/536/536-name-binds-to.html" rel="noopener noreferrer" target="_blank" title="">on this page</a>.</li><li>Read more about the AIMMS Cloud MySQL database <a href="https://documentation.aimms.com/cloud/db-config.html" rel="noopener noreferrer" target="_blank" title="">here</a>.</li></ul>`},
  "50": {hd: `I am using the retrieved data directly inside my AIMMS model without saving it first`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>Your data is stored in a database that requires a VPN setup to reach it;</li><li>you want to access this data directly from your AIMMS model.</li></ul><p><strong>Your to-do list to implement this route</strong>:</p><ol><li><a href="https://documentation.aimms.com/cloud/db-config.html" rel="noopener noreferrer" target="_blank" title="">Review this page</a> to understand the steps to follow to get the VPN setup realized.<br><br></li><li>After the VPN has been set up, use the&nbsp;<a href="https://documentation.aimms.com/functionreference/data-management/database-functions/sqlcreateconnectionstring.html#SQLCreateConnectionString" rel="noopener noreferrer" target="_blank" title="">SQLCreateConnectionString</a> method to create a connection to your database from within your AIMMS application. The URL to your database is needed for the ServerName argument.&nbsp;<br>[note: if you are using the MySQL database on the cloud, the direct URL to the database is always [yourcloudname].db.aimms.cloud]<br><br></li><li>To use the data from the database in your AIMMS model, you have two options:&nbsp;<ul><li>use the <a href="https://documentation.aimms.com/language-reference/data-communication-components/communicating-with-databases/index.html" rel="noopener noreferrer" target="_blank" title="">AIMMS Database functions</a> for reading.</li><li>create a DEX mapping that tells your application how the data in your database maps to the AIMMS model and from which database/tables it should be read. Read <a href="https://documentation.aimms.com/dataexchange/sqldb.html" rel="noopener noreferrer" target="_blank" title="">this page</a> to learn more.</li></ul></li></ol><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. Some common DEX-errors are described on&nbsp;<a href="https://documentation.aimms.com/dataexchange/troubleshoot.html" rel="noopener noreferrer" target="_blank" title="">this page</a>. &nbsp;<br>If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p>`},
  "51": {hd: `I'm saving the data in the Azure Data Lake Storage Gen2 first to use it from there`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>Your data is stored in a database that requires a VPN setup to reach it;</li><li>you want to save the data on the Azure Data Lake Storage Gen2 (ADLS) before further processing it.</li></ul><p><strong>Your to-do list to implement this route</strong>:</p><ol><li><a href="https://documentation.aimms.com/cloud/db-config.html" rel="noopener noreferrer" target="_blank" title="">Review this page</a> to understand the steps to follow to get the VPN setup realized.<br><br></li><li>Use the&nbsp;<a href="https://documentation.aimms.com/functionreference/data-management/database-functions/sqlcreateconnectionstring.html#SQLCreateConnectionString" rel="noopener noreferrer" target="_blank" title="">SQLCreateConnectionString</a> method to create a connection to your database from within your AIMMS application. The URL to your database is needed for the ServerName argument.&nbsp;<br>[note: if you are using the MySQL database on the cloud, the direct URL to the database is always [yourcloudname].db.aimms.cloud]<br><br></li><li>You will need to save the data from your database into a file that can be saved on the ADLS. Supported file formats are Parquet, CSV, JSON, XML and Excel.&nbsp;<br><br></li><li>Because you want to store the data on the ADLS, this may require you to create a file system structure on the ADLS that works for you if you didn&#39;t already implement one. Use our&nbsp;<a href="https://how-to.aimms.com/Articles/598/598-datalake-tools.html" rel="noopener noreferrer" target="_blank" title="">DataLake Tools Toolkit&nbsp;</a>to manage/create file systems.<br><br></li><li>Now you can transfer the created file to any location on the ADLS. Use the&nbsp;<a href="https://documentation.aimms.com/dataexchange/api.html#dex-dls-UploadFile" rel="noopener noreferrer" target="_blank" title="">dex::dls::UploadFile</a> method for this.<br><br></li><li>For the following steps, we will assume you are immediately using the retrieved data in your model.&nbsp;<br>If you want to use the data in the saved file in the future, use the&nbsp;<a href="https://documentation.aimms.com/dataexchange/api.html#dex-dls-DownloadFile" rel="noopener noreferrer" target="_blank" title="">dex::dls::DownloadFile</a> to download it to a local directory to process it further.&nbsp;<br><br></li><li>To use the data in your AIMMS model, you will need to create a mapping that tells your application how the data in your saved file maps to the AIMMS model and from which file it should be read. There are two ways to generate this mapping:<ul><li><strong><em>automated generation</em></strong> based on your existing model. <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#generate-mapping-file" rel="noopener noreferrer" target="_blank" title="">Read this chapter</a> to learn how to do this. We advise to, especially if you&#39;re doing this for the first time, do this locally (instead of on the cloud) so you can access the generated mappings easily and inspect/edit them when needed. Save the file within your project folder and upload it to the cloud as to be able to use it from there. Make sure to read <a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.</li><li><strong><em>manual generation</em></strong>. You will create the mapping yourself or use an automatically generated mapping and will edit it accordingly. There is a multitude of options available for your mapping. Make sure to read&nbsp;<a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.&nbsp;<br><br></li></ul></li><li>Add the mapping to your model (<a href="https://documentation.aimms.com/dataexchange/api.html#dex-AddMapping" rel="noopener noreferrer" target="_blank" title="">dex::AddMapping</a>) and use <a href="https://documentation.aimms.com/dataexchange/api.html#dex-ReadFromFile" rel="noopener noreferrer" target="_blank" title="">dex::ReadFromFile</a> as described in&nbsp;<a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#basic-setup" rel="noopener noreferrer" target="_blank" title="">this chapter of the how-to</a>. The first argument for dex::ReadFromFile will be your responseFile argument as used in step 3. The MappingName will be the mapping name you&#39;ve defined when you created/added the mapping via dex::AddMapping.&nbsp;</li></ol><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. Some common DEX-errors are described on&nbsp;<a href="https://documentation.aimms.com/dataexchange/troubleshoot.html" rel="noopener noreferrer" target="_blank" title="">this page</a>. &nbsp;<br>If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p>`},
  "52": {hd: `I'm saving the data in the AIMMS MySQL database first to use it from there`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>Your data is stored in a database that requires a VPN setup to reach it;</li><li>and you want to save the data in the AIMMS Cloud MySQL database to further process it from there.&nbsp;</li></ul><p><strong>Your to-do list to implement this route</strong>:</p><ol><li>Make sure the Data Exchange Library is installed. You will need to use it. If you don&#39;t know how to install a repository library,&nbsp;<a href="https://documentation.aimms.com/general-library/getting-started.html" rel="noopener noreferrer" target="_blank" title="">visit this page</a>.<br><br></li><li>Double check if your Cloud setup <a href="https://how-to.aimms.com/Articles/596/596-mysql-db-cloud.html" rel="noopener noreferrer" target="_blank" title="">has a MySQL database attached to it</a>. If you are in doubt, contact us at&nbsp;<a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.<br><br></li><li><a href="mailto:support@aimms.com" target="_blank" title=""></a><a href="https://documentation.aimms.com/cloud/db-config.html" rel="noopener noreferrer" target="_blank" title="">Review this page</a> to understand the steps to follow to get the VPN setup realized.<br><br></li><li>After the VPN has been set up, use the&nbsp;<a href="https://documentation.aimms.com/functionreference/data-management/database-functions/sqlcreateconnectionstring.html#SQLCreateConnectionString" rel="noopener noreferrer" target="_blank" title="">SQLCreateConnectionString</a> method to create a connection to your database from within your AIMMS application. The URL to your database is needed for the ServerName argument.&nbsp;<br>[note: if you are using the MySQL database on the cloud, the direct URL to the database is always [yourcloudname].db.aimms.cloud]<br><br></li><li>You will need to read the relevant data from the database into AIMMS identifiers within your model as to be able to save them in the MySQL Cloud database. For this, you have two options:&nbsp;<ul><li>use the <a href="https://documentation.aimms.com/language-reference/data-communication-components/communicating-with-databases/index.html" rel="noopener noreferrer" target="_blank" title="">AIMMS Database functions</a> for reading.</li><li>create a DEX mapping that tells your application how the data in your database maps to the AIMMS model and from which database/tables it should be read. Read&nbsp;<a href="https://documentation.aimms.com/dataexchange/sqldb.html" rel="noopener noreferrer" target="_blank" title="">this page</a> to learn more.<br><br></li></ul></li><li>To save the data on the MySQL Cloud database you should use the <a href="https://documentation.aimms.com/functionreference/data-management/database-functions/sqlcreateconnectionstring.html#SQLCreateConnectionString" rel="noopener noreferrer" target="_blank" title="">SQLCreateConnectionString</a> method to create a connection to your database from within your AIMMS application. The URL to your database (needed for the ServerName argument) on the cloud is always [yourcloudname].db.aimms.cloud. Once the connection has been established, you can use the&nbsp;<a href="https://documentation.aimms.com/language-reference/data-communication-components/communicating-with-databases/index.html" rel="noopener noreferrer" target="_blank" title="">AIMMS Database functions</a> to write.<br><br></li><li>To use the data from the database in your AIMMS model, you have two options:&nbsp;<ul><li>use the <a href="https://documentation.aimms.com/language-reference/data-communication-components/communicating-with-databases/index.html" rel="noopener noreferrer" target="_blank" title="">AIMMS Database functions</a> for reading.</li><li>create a DEX mapping that tells your application how the data in your database maps to the AIMMS model and from which database/tables it should be read. There are two ways to generate this mapping:<ul><li><strong><em>automated generation</em></strong> based on your existing model. <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#generate-mapping-file" rel="noopener noreferrer" target="_blank" title="">Read this chapter</a> to learn how to do this. We advise to do this locally (instead of on the cloud) so you can access the generated mappings easily and inspect/edit them when needed. Make sure to read <a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.</li><li><strong><em>manual generation</em></strong>. You will create the mapping yourself or use an automatically generated mapping and will edit it accordingly. There is a multitude of options available for your mapping. Make sure to read&nbsp;<a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.<br><br></li></ul></li></ul></li><li>[<em>only applicable if you are using a DEX mapping</em>] Add the mapping to your model (<a href="https://documentation.aimms.com/dataexchange/api.html#dex-AddMapping" rel="noopener noreferrer" target="_blank" title="">dex::AddMapping</a>) and use <a href="https://documentation.aimms.com/dataexchange/api.html#dex-ReadFromFile" rel="noopener noreferrer" target="_blank" title="">dex::ReadFromFile</a> as described in <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#basic-setup" rel="noopener noreferrer" target="_blank" title="">this chapter of the how-to</a>. The first argument for dex::ReadFromFile will be the file you saved in the previous step. The MappingName will be the mapping name you&#39;ve defined when you created/added the mapping via dex::AddMapping.</li></ol><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. Some common DEX-errors are described on <a href="https://documentation.aimms.com/dataexchange/troubleshoot.html" rel="noopener noreferrer" target="_blank" title="">this page</a>. &nbsp;</p><p>If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p><p><strong>Further reading/resources/additional information</strong></p><ul><li><a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">On this page</a> you will find a description of the different mapping possibilities for the different supported formats. Read it along <a href="https://documentation.aimms.com/dataexchange/standard.html" rel="noopener noreferrer" target="_blank" title="">with this page</a> to get a full understanding on how the mapping works.</li><li><a href="https://documentation.aimms.com/dataexchange/api.html#methods-for-reading-and-writing-data" rel="noopener noreferrer" target="_blank" title="">Here</a> you can find all the methods you can and should use to generate and use the mapping.</li><li>Find an illustrative example of a mapping <a href="https://how-to.aimms.com/Articles/536/536-name-binds-to.html" rel="noopener noreferrer" target="_blank" title="">on this page</a>.</li><li>Read more about the AIMMS Cloud MySQL database <a href="https://documentation.aimms.com/cloud/db-config.html" rel="noopener noreferrer" target="_blank" title="">here</a>.</li></ul>`},
  "53": {hd: `The data is stored on the Azure Data Lake Storage Gen2`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>Your data is stored on the Azure Data Lake Storage Gen2 (ADLS) and you want to process it from there.</li></ul><p><strong>Your to-do list to implement this route</strong>:<br></p><ol><li>Use the <a href="https://documentation.aimms.com/dataexchange/api.html#dex-dls-DownloadFile" rel="noopener noreferrer" target="_blank" title="">dex::dls::DownloadFile</a> to download the data you want to you use to a local directory to process it further.&nbsp;<br><br></li><li>To use the data in your AIMMS model, you will need to create a mapping that tells your application how the data in your saved file maps to the AIMMS model and from which file it should be read. There are two ways to generate this mapping:<ul><li><strong><em>automated generation</em></strong> based on your existing model. <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#generate-mapping-file" rel="noopener noreferrer" target="_blank" title="">Read this chapter</a> to learn how to do this. We advise to, especially if you&#39;re doing this for the first time, do this locally (instead of on the cloud) so you can access the generated mappings easily and inspect/edit them when needed. Save the file within your project folder and upload it to the cloud as to be able to use it from there. Make sure to read <a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.</li><li><strong><em>manual generation</em></strong>. You will create the mapping yourself or use an automatically generated mapping and will edit it accordingly. There is a multitude of options available for your mapping. Make sure to read&nbsp;<a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.&nbsp;<br><br></li></ul></li><li>Add the mapping to your model (<a href="https://documentation.aimms.com/dataexchange/api.html#dex-AddMapping" rel="noopener noreferrer" target="_blank" title="">dex::AddMapping</a>) and use <a href="https://documentation.aimms.com/dataexchange/api.html#dex-ReadFromFile" rel="noopener noreferrer" target="_blank" title="">dex::ReadFromFile</a> as described in&nbsp;<a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#basic-setup" rel="noopener noreferrer" target="_blank" title="">this chapter of the how-to</a>. The first argument for dex::ReadFromFile will be your responseFile argument as used in step 3. The MappingName will be the mapping name you&#39;ve defined when you created/added the mapping via dex::AddMapping.&nbsp;</li></ol><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. Some common DEX-errors are described on&nbsp;<a href="https://documentation.aimms.com/dataexchange/troubleshoot.html" rel="noopener noreferrer" target="_blank" title="">this page</a>. &nbsp;<br>If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p>`},
  "54": {hd: `The data is stored on the AIMMS Cloud MySQL database`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>Your data is stored in the AIMMS Cloud MySQL database and you want to further process it from there.&nbsp;</li></ul><p><strong>Your to-do list to implement this route</strong>:</p><ol><li>Make sure the Data Exchange Library is installed. You will need to use it. If you don&#39;t know how to install a repository library,&nbsp;<a href="https://documentation.aimms.com/general-library/getting-started.html" rel="noopener noreferrer" target="_blank" title="">visit this page</a>.<br><br></li><li>Double check if your Cloud setup <a href="https://how-to.aimms.com/Articles/596/596-mysql-db-cloud.html" rel="noopener noreferrer" target="_blank" title="">has a MySQL database attached to it</a>. If you are in doubt, contact us at&nbsp;<a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.<br><br></li><li>Use the&nbsp;<a href="https://documentation.aimms.com/functionreference/data-management/database-functions/sqlcreateconnectionstring.html#SQLCreateConnectionString" rel="noopener noreferrer" target="_blank" title="">SQLCreateConnectionString</a> method to create a connection to your database from within your AIMMS application. The URL to your database is needed for the ServerName argument.&nbsp;<br>[note: if you are using the MySQL database on the cloud, the direct URL to the database is always [yourcloudname].db.aimms.cloud]<br><br></li><li>You will need to read the relevant data from the database into AIMMS identifiers within your model as to be able to save them in the MySQL Cloud database. For this, you have two options:&nbsp;<ul><li>use the <a href="https://documentation.aimms.com/language-reference/data-communication-components/communicating-with-databases/index.html" rel="noopener noreferrer" target="_blank" title="">AIMMS Database functions</a> for reading.</li><li>create a DEX mapping that tells your application how the data in your database maps to the AIMMS model and from which database/tables it should be read. Read&nbsp;<a href="https://documentation.aimms.com/dataexchange/sqldb.html" rel="noopener noreferrer" target="_blank" title="">this page</a> to learn more.<br><br></li></ul></li><li>To save the data on the MySQL Cloud database you should use the <a href="https://documentation.aimms.com/functionreference/data-management/database-functions/sqlcreateconnectionstring.html#SQLCreateConnectionString" rel="noopener noreferrer" target="_blank" title="">SQLCreateConnectionString</a> method to create a connection to your database from within your AIMMS application. The URL to your database (needed for the ServerName argument) on the cloud is always [yourcloudname].db.aimms.cloud. Once the connection has been established, you can use the&nbsp;<a href="https://documentation.aimms.com/language-reference/data-communication-components/communicating-with-databases/index.html" rel="noopener noreferrer" target="_blank" title="">AIMMS Database functions</a> to write.<br><br></li><li>To use the data from the database in your AIMMS model, you have two options:&nbsp;<ul><li>use the <a href="https://documentation.aimms.com/language-reference/data-communication-components/communicating-with-databases/index.html" rel="noopener noreferrer" target="_blank" title="">AIMMS Database functions</a> for reading.</li><li>create a DEX mapping that tells your application how the data in your database maps to the AIMMS model and from which database/tables it should be read. There are two ways to generate this mapping:<ul><li><strong><em>automated generation</em></strong> based on your existing model. <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#generate-mapping-file" rel="noopener noreferrer" target="_blank" title="">Read this chapter</a> to learn how to do this. We advise to do this locally (instead of on the cloud) so you can access the generated mappings easily and inspect/edit them when needed. Make sure to read <a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.</li><li><strong><em>manual generation</em></strong>. You will create the mapping yourself or use an automatically generated mapping and will edit it accordingly. There is a multitude of options available for your mapping. Make sure to read&nbsp;<a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.<br><br></li></ul></li></ul></li><li>[<em>only applicable if you are using a DEX mapping</em>] Add the mapping to your model (<a href="https://documentation.aimms.com/dataexchange/api.html#dex-AddMapping" rel="noopener noreferrer" target="_blank" title="">dex::AddMapping</a>) and use <a href="https://documentation.aimms.com/dataexchange/api.html#dex-ReadFromFile" rel="noopener noreferrer" target="_blank" title="">dex::ReadFromFile</a> as described in <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#basic-setup" rel="noopener noreferrer" target="_blank" title="">this chapter of the how-to</a>. The first argument for dex::ReadFromFile will be the file you saved in the previous step. The MappingName will be the mapping name you&#39;ve defined when you created/added the mapping via dex::AddMapping.</li></ol><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. Some common DEX-errors are described on <a href="https://documentation.aimms.com/dataexchange/troubleshoot.html" rel="noopener noreferrer" target="_blank" title="">this page</a>.&nbsp;</p><p>If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p><p><strong>Further reading/resources/additional information</strong></p><ul><li><a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">On this page</a> you will find a description of the different mapping possibilities for the different supported formats. Read it along <a href="https://documentation.aimms.com/dataexchange/standard.html" rel="noopener noreferrer" target="_blank" title="">with this page</a> to get a full understanding on how the mapping works.</li><li><a href="https://documentation.aimms.com/dataexchange/api.html#methods-for-reading-and-writing-data" rel="noopener noreferrer" target="_blank" title="">Here</a> you can find all the methods you can and should use to generate and use the mapping.</li><li>Find an illustrative example of a mapping <a href="https://how-to.aimms.com/Articles/536/536-name-binds-to.html" rel="noopener noreferrer" target="_blank" title="">on this page</a>.</li><li>Read more about the AIMMS Cloud MySQL database <a href="https://documentation.aimms.com/cloud/db-config.html" rel="noopener noreferrer" target="_blank" title="">here</a>.</li></ul>`},
  "55": {hd: `I want to push the data from AIMMS to my target`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>You want to push data from your AIMMS model directly to your target;</li><li>and you can PUT the data in JSON, XML, Parquet, database or CSV format via API.</li></ul><p><strong>How does it work?</strong></p><ol><li>Make sure the Data Exchange Library is installed. You will need to use it. If you don&#39;t know how to install a repository library,&nbsp;<a href="https://documentation.aimms.com/general-library/getting-started.html" rel="noopener noreferrer" target="_blank" title="">visit this page</a>.<br><br></li><li>Create a DEX mapping that tells your application how the data from your AIMMS model maps to a file format of your choice. There are two ways to generate this mapping:<ul><li><strong><em>automated generation</em></strong> based on your existing model. <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#generate-mapping-file" rel="noopener noreferrer" target="_blank" title="">Read this chapter</a> to learn how to do this. We advise to do this locally (instead of on the cloud) so you can access the generated mappings easily and inspect/edit them when needed. Make sure to read <a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.</li><li><strong><em>manual generation</em></strong>. You will create the mapping yourself or use an automatically generated mapping and will edit it accordingly. There is a multitude of options available for your mapping. Make sure to read <a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.</li></ul><br></li><li>Use the <a href="https://documentation.aimms.com/dataexchange/api.html#dex-WriteToFile" rel="noopener noreferrer" target="_blank" title="">dex::WriteToFile</a> to use your mapping to write the needed data to a file or&nbsp;<a href="https://documentation.aimms.com/dataexchange/api.html#memory-streams" rel="noopener noreferrer" target="_blank" title="">memory stream</a>.<br><br></li><li>Use the Data Exchange Library (DEX) to call the REST API to push the prepared data file/memory stream. A function that you will need to use for this is the <a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-NewRequest" rel="noopener noreferrer" target="_blank" title=""><strong>dex::client::NewRequest</strong></a>, in which you can use URLs of the REST API of your source and add additional parameters to make sure you are building the request correctly. An example of this flow can be found in&nbsp;<a href="https://how-to.aimms.com/Articles/533/533-how-to-retrieve-file-data-with-data-exchange.html" rel="noopener noreferrer" target="_blank" title="">this how-to article</a>.<br><br></li><li>Use&nbsp;<a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-PerformRequest" rel="noopener noreferrer" target="_blank"><strong>dex::client::PerformRequest</strong></a> to actually perform the request you&#39;ve built.<br><br></li><li>And <a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-CloseRequest" rel="noopener noreferrer" target="_blank"><strong>dex::client::CloseRequest</strong></a> to properly close the request you&#39;ve built.</li></ol>`},
  "56": {hd: `I want to push the data from AIMMS to my target`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>You want to push data from your AIMMS Cloud MySQL database directly to your target;</li><li>you have an API available that can be called to receive the data;</li><li>and you can PUT the data in JSON, XML, Parquet, database or CSV format via API.</li></ul><p><strong>How does it work?</strong></p><ol><li>Make sure the Data Exchange Library is installed. You will need to use it. If you don&#39;t know how to install a repository library,&nbsp;<a href="https://documentation.aimms.com/general-library/getting-started.html" rel="noopener noreferrer" target="_blank" title="">visit this page</a>.<br><br></li><li>Create a DEX mapping that tells your application how the data from your database maps to a file format of your choice. There are two ways to generate this mapping:<ul><li><strong><em>automated generation</em></strong> based on your existing model. <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#generate-mapping-file" rel="noopener noreferrer" target="_blank" title="">Read this chapter</a> to learn how to do this. We advise to do this locally (instead of on the cloud) so you can access the generated mappings easily and inspect/edit them when needed. Make sure to read <a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.</li><li><strong><em>manual generation</em></strong>. You will create the mapping yourself or use an automatically generated mapping and will edit it accordingly. There is a multitude of options available for your mapping. Make sure to read&nbsp;<a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.<br><br></li></ul></li><li>Use the <a href="https://documentation.aimms.com/dataexchange/api.html#dex-WriteToFile" rel="noopener noreferrer" target="_blank" title="">dex::WriteToFile</a> to use your mapping to write the needed data to a file or&nbsp;<a href="https://documentation.aimms.com/dataexchange/api.html#memory-streams" rel="noopener noreferrer" target="_blank" title="">memory stream</a>.<br><br></li><li>Use the Data Exchange Library (DEX) to call the REST API to push the prepared data file/memory stream. A function that you will need to use for this is the <a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-NewRequest" rel="noopener noreferrer" target="_blank" title=""><strong>dex::client::NewRequest</strong></a>, in which you can use URLs of the REST API of your source and add additional parameters to make sure you are building the request correctly. An example of this flow can be found in&nbsp;<a href="https://how-to.aimms.com/Articles/533/533-how-to-retrieve-file-data-with-data-exchange.html" rel="noopener noreferrer" target="_blank" title="">this how-to article</a>.<br><br></li><li>Use&nbsp;<a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-PerformRequest" rel="noopener noreferrer" target="_blank"><strong>dex::client::PerformRequest</strong></a> to actually perform the request you&#39;ve built.<br><br></li><li>And <a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-CloseRequest" rel="noopener noreferrer" target="_blank"><strong>dex::client::CloseRequest</strong></a> to properly close the request you&#39;ve built.</li></ol><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. Some common DEX-errors are described on <a href="https://documentation.aimms.com/dataexchange/troubleshoot.html" rel="noopener noreferrer" target="_blank" title="">this page</a>. &nbsp;If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p><p><strong>Further reading/resources/additional information</strong></p><ul><li><a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">On this page</a> you will find a description of the different mapping possibilities for the different supported formats. Read it along <a href="https://documentation.aimms.com/dataexchange/standard.html" rel="noopener noreferrer" target="_blank" title="">with this page</a> to get a full understanding on how the mapping works.</li><li><a href="https://documentation.aimms.com/dataexchange/api.html#methods-for-reading-and-writing-data" rel="noopener noreferrer" target="_blank" title="">Here</a> you can find all the methods you can and should use to generate and use the mapping.</li><li>Find an illustrative example of a mapping <a href="https://how-to.aimms.com/Articles/536/536-name-binds-to.html" rel="noopener noreferrer" target="_blank" title="">on this page</a>.</li><li>Read more about the AIMMS Cloud MySQL database <a href="https://documentation.aimms.com/cloud/db-config.html" rel="noopener noreferrer" target="_blank" title="">here</a>.</li></ul>`},
  "57": {hd: `I want to pull the data: the target will manage the request`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>You want to pull data from your AIMMS Cloud MySQL database;</li><li>and you can handle JSON, XML, Parquet, database or CSV formatted data.</li></ul><p>In this scenario we offer the option to utilize the <strong>AIMMS Azure Data Lake Storage Gen2 (ADLS)</strong>, and its related Azure-native API&#39;s to exchange the data.&nbsp;</p><p><strong>How does it work?</strong></p><p>If you want to pull the data via the target, you need to make the data available via the Azure Data Lake Storage account that is by default part of your cloud. In <a href="https://how-to.aimms.com/Articles/594/594-adls-data-integration.html" rel="noopener noreferrer" target="_blank" title="">this how-to article</a> we describe how to do this. You might need to follow these steps to transfer the data from your MySQL database to a file that can be stored on the ADLS:</p><ol><li>Create a DEX mapping that tells your application how the data from your database maps to a file format of your choice. There are two ways to generate this mapping:<ul><li><strong><em>automated generation</em></strong> based on your existing model. <a href="https://how-to.aimms.com/Articles/528/528-how-to-set-up-data-exchange-basics.html#generate-mapping-file" rel="noopener noreferrer" target="_blank" title="">Read this chapter</a> to learn how to do this. We advise to do this locally (instead of on the cloud) so you can access the generated mappings easily and inspect/edit them when needed. Make sure to read <a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.</li><li><strong><em>manual generation</em></strong>. You will create the mapping yourself or use an automatically generated mapping and will edit it accordingly. There is a multitude of options available for your mapping. Make sure to read&nbsp;<a href="https://documentation.aimms.com/dataexchange/mapping.html" rel="noopener noreferrer" target="_blank" title="">through this page</a> to understand your options.<br><br></li></ul></li><li>Use the <a href="https://documentation.aimms.com/dataexchange/api.html#dex-WriteToFile" rel="noopener noreferrer" target="_blank" title="">dex::WriteToFile</a> to use your mapping to write the needed data to a file or <a href="https://documentation.aimms.com/dataexchange/api.html#memory-streams" rel="noopener noreferrer" target="_blank" title="">memory stream</a>.</li></ol><p><strong>Accessing the ADLS from an external source</strong></p><p>You can access the ADLS from any external location by utilizing these two types of Azure API&#39;s:</p><ul><li><a href="https://learn.microsoft.com/en-us/rest/api/storageservices/data-lake-storage-gen2" rel="noopener noreferrer" target="_blank" title="">The Azure Data Lake Storage Gen2 REST API</a> - used for creating and managing so-called filesystems and paths (used for structuring your storage)</li><li><a href="https://learn.microsoft.com/en-us/rest/api/storageservices/blob-service-rest-api" rel="noopener noreferrer" target="_blank" title="">The Azure Blob Storage REST API</a> - used for pulling &#39;blobs&#39; (or: data/files) from the chosen location on the storage. There is a <a href="https://learn.microsoft.com/en-us/rest/api/storageservices/get-blob?tabs=microsoft-entra-id" rel="noopener noreferrer" target="_blank">&#39;GET Blob&#39; request</a> with which you can pull any file from the storage via SAS tokens.</li></ul><p>You can use <a href="https://how-to.aimms.com/Articles/598/598-datalake-tools.html" rel="noopener noreferrer" target="_blank" title="">our DataLake Tools toolkit</a> to help you create the needed SAS-tokens to access your ADLS as part of your AIMMS Cloud subscription. In <a href="https://how-to.aimms.com/Articles/595/595-accessing-the-azure-datalake-externally.html" rel="noopener noreferrer" target="_blank" title="">this how-to article</a> you&#39;ll find an example on how to implement this route.</p><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p>`},
  "58": {hd: `I want to access the data directly from a remote server`, type: 'r', h: `<p>If you want to access the data in the AIMMS Cloud MySQL database directly from a remote server, you will need a VPN. <a href="https://documentation.aimms.com/cloud/db-config.html#aimms-cloud-database-and-vpn-configuration" rel="noopener noreferrer" target="_blank" title="">On this page</a> you will find all the details about this.</p><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p>`},
  "59": {hd: `I want to access the data directly from my local computer`, type: 'r', h: `<p>If you want to access the data directly from your local computer, you can use <a href="https://documentation.aimms.com/cloud/db-config.html#database-tunnel-application" rel="noopener noreferrer" target="_blank" title="">the AIMMS DB Tunnel App</a>.</p><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p>`},
  "60": {hd: `I want to push the data from AIMMS to my target`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>You want to push data from your AIMMS PRO Storage directly to your target;</li><li>you have an API available that can be called to receive the data;</li><li>and you can PUT the data in JSON, XML, Parquet, database or CSV format via API.</li></ul><p><strong>How does it work?</strong></p><ol><li>Make sure the Data Exchange Library is installed. You will need to use it. If you don&#39;t know how to install a repository library,&nbsp;<a href="https://documentation.aimms.com/general-library/getting-started.html" rel="noopener noreferrer" target="_blank" title="">visit this page</a>.<br><br></li><li>Download the file using the&nbsp;<a href="https://documentation.aimms.com/pro/pro-data-man.html#transferring-files" rel="noopener noreferrer" target="_blank" title="">pro::RetrieveFileFromCentralStorage</a>.<br><br></li><li>Use the Data Exchange Library (DEX) to call the REST API to push the downloaded data file. A function that you will need to use for this is the <a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-NewRequest" rel="noopener noreferrer" target="_blank" title=""><strong>dex::client::NewRequest</strong></a>, in which you can use URLs of the REST API of your source and add additional parameters to make sure you are building the request correctly. An example of this flow can be found in&nbsp;<a href="https://how-to.aimms.com/Articles/533/533-how-to-retrieve-file-data-with-data-exchange.html" rel="noopener noreferrer" target="_blank" title="">this how-to article</a>.<br><br></li><li>Use&nbsp;<a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-PerformRequest" rel="noopener noreferrer" target="_blank"><strong>dex::client::PerformRequest</strong></a> to actually perform the request you&#39;ve built.<br><br></li><li>And <a href="https://documentation.aimms.com/dataexchange/api.html#dex-client-CloseRequest" rel="noopener noreferrer" target="_blank"><strong>dex::client::CloseRequest</strong></a> to properly close the request you&#39;ve built.</li></ol><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. Some common DEX-errors are described on <a href="https://documentation.aimms.com/dataexchange/troubleshoot.html" rel="noopener noreferrer" target="_blank" title="">this page</a>. &nbsp;If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p>`},
  "61": {hd: `I want to pull the data: the target will manage the request`, type: 'r', h: `<p><strong>To recap:</strong></p><ul><li>You want to pull data from your AIMMS PRO Storage;</li><li>and you can handle JSON, XML, Parquet, database or CSV formatted data.</li></ul><p>In this scenario we offer the option to utilize the <strong>AIMMS Azure Data Lake Storage Gen2 (ADLS)</strong>, and its related Azure-native API&#39;s to exchange the data.&nbsp;</p><p><strong>How does it work?</strong></p><p>If you want to pull the data via the target, you need to make the data available via the Azure Data Lake Storage account that is by default part of your cloud. In <a href="https://how-to.aimms.com/Articles/594/594-adls-data-integration.html" rel="noopener noreferrer" target="_blank" title="">this how-to article</a> we describe how to do this. You might need to download the file from the storage first using the <a href="https://documentation.aimms.com/pro/pro-data-man.html#transferring-files" rel="noopener noreferrer" target="_blank" title="">pro::RetrieveFileFromCentralStorage</a>.</p><p><strong>Accessing the ADLS from an external source</strong></p><p>You can access the ADLS from any external location by utilizing these two types of Azure API&#39;s:</p><ul><li><a href="https://learn.microsoft.com/en-us/rest/api/storageservices/data-lake-storage-gen2" rel="noopener noreferrer" target="_blank" title="">The Azure Data Lake Storage Gen2 REST API</a> - used for creating and managing so-called filesystems and paths (used for structuring your storage)</li><li><a href="https://learn.microsoft.com/en-us/rest/api/storageservices/blob-service-rest-api" rel="noopener noreferrer" target="_blank" title="">The Azure Blob Storage REST API</a> - used for pulling &#39;blobs&#39; (or: data/files) from the chosen location on the storage. There is a <a href="https://learn.microsoft.com/en-us/rest/api/storageservices/get-blob?tabs=microsoft-entra-id" rel="noopener noreferrer" target="_blank">&#39;GET Blob&#39; request</a> with which you can pull any file from the storage via SAS tokens.</li></ul><p>You can use <a href="https://how-to.aimms.com/Articles/598/598-datalake-tools.html" rel="noopener noreferrer" target="_blank" title="">our DataLake Tools toolkit</a> to help you create the needed SAS-tokens to access your ADLS as part of your AIMMS Cloud subscription. In <a href="https://how-to.aimms.com/Articles/595/595-accessing-the-azure-datalake-externally.html" rel="noopener noreferrer" target="_blank" title="">this how-to article</a> you&#39;ll find an example on how to implement this route.</p><p><strong>Error handling</strong></p><p>There is always a possibility you will encounter errors while setting up this route. If you get stuck, reach out to us via <a href="https://community.aimms.com/" rel="noopener noreferrer" target="_blank" title="">our community</a>, or send us an email on <a href="mailto:support@aimms.com" target="_blank" title="">support@aimms.com</a>.</p>`}
	};
	var hist=[],crumbs=[],cur='1',el=document.getElementById('di-helper');
	function styleResult(h){
	  var A='<div class="di-admon"><p class="di-admon-title">',B='</p><div class="di-admon-body">',C='</div></div>';
	  h=h.replace(/<p><strong>To recap:<\/strong><\/p>\s*(<ul>[\s\S]*?<\/ul>)/,A+'To recap'+B+'$1'+C);
	  h=h.replace(/<p><strong>Further reading[^<]*<\/strong><\/p>([\s\S]*)$/,'<div class="di-admon di-admon-see"><p class="di-admon-title">See also'+B+'$1'+C);
	  return h;
	}
	function render(id){
	  var n=T[id],s='';
	  if(crumbs.length){
	    s+='<div class="di-crumb">';
	    crumbs.forEach(function(c,i){
	      if(i)s+='<span class="di-crumb-sep">&#8250;</span>';
	      s+='<span class="di-crumb-item" title="'+c+'">'+c+'</span>';
	    });
	    s+='</div>';
	  }
	  if(n.type==='r')s+='<div class="di-hd">Recommended Approach</div>';
	  if(n.type==='q'){
	    if(n.q)s+='<p class="di-q">'+n.q+'</p>';
	    if(n.content)s+='<div class="di-content">'+n.content+'</div>';
	    s+='<div class="di-opts">';
	    n.c.forEach(function(c,i){
	      s+='<button class="di-btn" title="'+c.tip+'" onclick="diGo(\''+id+'\','+i+')">'+c.l+'</button>';
	    });
	    s+='</div>';
	  }else{
	    if(n.h)s+='<div class="di-res">'+styleResult(n.h)+'</div>';
	  }
	  if(hist.length)s+='<div class="di-nav"><button onclick="diBack()">&#8592; Back</button><button onclick="diReset()">&#8635; Restart</button></div>';
	  el.innerHTML=s;
	}
	window.diGo=function(id,i){hist.push(cur);crumbs.push(T[id].c[i].l);cur=T[id].c[i].to;render(cur);};
	window.diBack=function(){if(hist.length){cur=hist.pop();crumbs.pop();render(cur);}};
	window.diReset=function(){hist=[];crumbs=[];cur='1';render(cur);};
	render('1');
	}());
	</script>

|

Native Options for Storing Data
--------------------------------

There are three AIMMS-native ways to store data within your AIMMS Cloud (all included in the above decision tree):

#.	**RECOMMENDED:** Every AIMMS Cloud is by default equipped with an `Azure Data Lake Storage Gen2 (ADLS) <https://documentation.aimms.com/dataexchange/dls.html>`_ that you can use to store data on and exchange data with.
#.	Another option is to equip your cloud account with `a MySQL database <https://documentation.aimms.com/cloud/db-config.html>`_ for an additional fee.
#.	Your AIMMS Cloud has a so-called `PRO Storage <https://documentation.aimms.com/pro/pro-data-man.html>`_ available on which (case) files can be saved.

If you have questions regarding AIMMS data integration or you're stuck implementing one of the methods, feel free to contact us via support@aimms.com. We are here to help.

.. seealso::

	#.	Read all about `our Data Exchange Library <https://documentation.aimms.com/dataexchange/index.html>`_ here. It includes a section about the `Azure Data Lake Storage <https://documentation.aimms.com/dataexchange/dls.html>`_.
	#.	On our `AIMMS Cloud Database + VPN page <https://documentation.aimms.com/cloud/db-config.html>`_ you will find all the details on this integration method. 
	#.	`Our how-to platform <https://how-to.aimms.com/C_Developer/Sub_Connectivity/index.html>`_ provides a wide range of articles with examples on data integration.


.. spelling:word-list::

    dex
    mappingfile
    mappingfiles
    mappingname
	ADLS
	azure
	mysql
	aimms
	how-to
	